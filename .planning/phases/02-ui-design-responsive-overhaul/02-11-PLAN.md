---
phase: 02-ui-design-responsive-overhaul
plan: 11
type: execute
wave: 4
depends_on: [02-01, 02-08, 02-09]
files_modified:
  - app/listen/page.js
  - app/session/page.js
  - lib/tokens.js
autonomous: true
requirements: [UI-03, UI-04, UI-05]
must_haves:
  truths:
    - "Listen is fully usable at 360px: panes stack (D-05) — player card with 140px art tile, 'Up next' as a chip row under the player, sections list, then quote card; the local sidebar is hidden on phone because the global bottom tab bar covers navigation"
    - "Listen's light/dark toggle still works, now reading from token pairs (§7.5) instead of ad-hoc hex values"
    - "Session is fully usable at 360px: the 72px left rail retires on phone in favor of the global bottom tab bar, the lime hero goes full-width, waiting rows stack, and the shelf is a 2-column grid"
    - "Session greets 'Good morning, Iven' — LEARNER_NAME imported from lib/store.js (02-09); the local 'Sam' constant is deleted"
    - "All Phase 1 Links survive on both screens: Listen sidebar (desktop), Read instead, Quiz me after, Session rail (desktop), hero CTAs, waiting rows — every href still encoded"
  artifacts:
    - path: "lib/tokens.js"
      provides: "tokens.listenLight / tokens.listenDark palette pairs (§7.5) used only by Listen"
      contains: "listenDark"
    - path: "app/listen/page.js"
      provides: "Responsive Listen with tokenized palette, TabBar on phone, a11y'd player controls"
      contains: "TabBar"
    - path: "app/session/page.js"
      provides: "Responsive Session with TabBar on phone and the shared learner name"
      contains: "TabBar"
  key_links:
    - from: "app/listen/page.js"
      to: "components/TabBar.js"
      via: "renders <TabBar/> as the last element (Pattern 4 — Listen lives outside AppShell)"
      pattern: "TabBar"
    - from: "app/session/page.js"
      to: "lib/store.js"
      via: "import { LEARNER_NAME }"
      pattern: "LEARNER_NAME"
    - from: "app/listen/page.js"
      to: "/subjects/{subject}"
      via: "existing 'Read instead' encoded Link (D-11)"
      pattern: "Read instead"
---

<objective>
Responsive-overhaul the two screens that live outside the (dashboard) group. Listen: panes stack on phone (D-05), the 212px art tile and 330px quote card stop blowing out 360px, the local palette object becomes §7.5 token pairs, transport controls get aria-labels, and the phone gets the global TabBar. Session: the left rail retires on phone for the TabBar (its destinations stay reachable via subject pages per the spec note), the shelf reflows 4→2→2-col, and the Sam/Iven split dies by importing LEARNER_NAME.

Purpose: UI-03 (the two widest-content screens at 360px), UI-04 (§14.3 copy where triggerable), UI-05 (§7.5 dark tokens complete the system). D-11 fence: every Link and the ?subject= context survive.

Output: reworked `app/listen/page.js`, `app/session/page.js`, and a small additive change to `lib/tokens.js` (listen palettes). No audio, no data wiring (AUDIO-01/DATA are deferred).
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ui-design-responsive-overhaul/02-CONTEXT.md
@.planning/phases/02-ui-design-responsive-overhaul/02-RESEARCH.md
@app/listen/page.js
@app/session/page.js
@lib/tokens.js

<interfaces>
From lib/tokens.js (after this plan's Task 0 addition):
```js
export const tokens            // existing map — plus:
tokens.listenLight = { pageBg:'#f3f2f9', cardBg:'#ffffff', textPrimary:'#12121a', textSecondary:'#6e6c82',
                        textFaint:'#a09db2', rowBg:'#f6f5fb', divider:'#f0eff6', chipBg:'#12121a', chipText:'#ffffff', accentTint:'#efe9ff' }
tokens.listenDark  = { pageBg:'#0e0e14', cardBg:'#1a1a24', textPrimary:'#ffffff', textSecondary:'rgba(255,255,255,.55)',
                       textFaint:'rgba(255,255,255,.45)', rowBg:'rgba(255,255,255,.08)', divider:'rgba(255,255,255,.08)',
                       chipBg:'#ffffff', chipText:'#12121a', accentTint:'rgba(108,60,233,.25)' }
// §7.5: values mapped from the current listen palette onto §7.2 tokens; used ONLY by Listen; full-app dark mode stays deferred
export function useViewport()      // 'phone' drives pane stacking + rail retirement
```
From components (02-08):
```jsx
<TabBar />   // 'use client', fixed bottom bar <768px, five destinations, self-hiding — render as the LAST element of each screen's tree
```
From lib/store.js (02-09):
```js
export const LEARNER_NAME = 'Iven'
```
CSS classes (globals.css from 02-01): `.btn .btn-primary .btn-secondary .btn-ghost .btn-undone .on-dark` (lime focus ring inside dark surfaces — Listen dark mode's interactive controls get `on-dark`), `.skeleton`.
OWNERSHIP RULE: no inline background/color on .btn-*/.on-dark carriers.
Dev-server protocol: scratch port 3211; `node scripts/check-viewports.mjs --port 3211 --routes /listen?subject=AI%20Agents,/session`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Add §7.5 listen palette token pairs to lib/tokens.js</name>
  <files>lib/tokens.js</files>
  <read_first>
  - .planning/phases/02-ui-design-responsive-overhaul/02-UI-SPEC.md — §7.5 (Listen's toggle KEPT and TOKENIZED: palette becomes two token pairs used only by Listen; full-app dark mode deferred), §7.2 (the canonical values the pairs map onto — onDarkSecondary rgba(255,255,255,.55), inkDeep #0e0e14, etc.)
  - lib/tokens.js (the 02-01 contract — additive change only; do not alter existing exports)
  - app/listen/page.js (the current palette object being lifted — lines defining `palette`)
  </read_first>
  <action>
Add to the existing `tokens` object (or as sibling exported constants merged in — follow the file's established shape): `listenLight` and `listenDark` maps exactly as specified in this plan's `<interfaces>` block. Rules: values map the current screen palette onto §7.2 tokens (e.g. dark textSecondary rgba(255,255,255,.55) IS `color.onDarkSecondary`; dark pageBg IS `color.inkDeep` — reference them as `color.inkDeep` etc. inside the pair definitions rather than duplicating hex where a §7.2 name exists); the pairs are consumed ONLY by Listen (§7.5 scope); light-mode values reuse existing token values (canvas/surface/etc.). No other file changes; existing exports and their shapes stay byte-compatible (02-01's acceptance test `import('./lib/tokens.js')` must still pass).
  </action>
  <verify>
    <automated>cd /Volumes/1TBFULLYPAID/Projects/yahshua/learnit && npm run build && grep -q "listenDark" lib/tokens.js && grep -q "listenLight" lib/tokens.js && grep -q "export const tokens" lib/tokens.js && node -e "import('./lib/tokens.js').then(m => { if (!m.tokens.listenDark || !m.tokens.listenLight || m.tokens.status.done.fg !== '#15703c') process.exit(1); console.log('LISTEN-TOKENS-OK') })" && echo TOKENS-LISTEN-OK</automated>
  </verify>
  <done>tokens.js carries both palette pairs; build passes; the 02-01 import contract still holds.</done>
  <acceptance_criteria>
  - `node -e "import('./lib/tokens.js').then(m=>console.log(m.tokens.listenDark.pageBg))"` prints `#0e0e14`
  - `! grep -q "use client" lib/tokens.js` still true (directive-less invariant preserved)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 1: Listen responsive — pane stacking, TabBar, tokenized palette, a11y transport</name>
  <files>app/listen/page.js</files>
  <read_first>
  - .planning/phases/02-ui-design-responsive-overhaul/02-UI-SPEC.md — §12 row 8 (Listen layouts: desktop 3-zone proportions KEPT (D-05); tablet sidebar 250 stays, sections/quote stack; phone panes stack — local sidebar nav HIDDEN (global bottom bar covers it), 'Up next' becomes a chip row under the player, player card with 140px art tile, sections list, quote card; dark toggle stays), §7.1 (accent reserved uses: Listen hero art tile + active section row tint), §11.3 (waveforms aria-hidden; player buttons aria-label 'Play', 'Skip back 15 seconds'), §14.3 (Listen copy)
  - .planning/phases/02-ui-design-responsive-overhaul/02-CONTEXT.md — D-05, D-06, D-11; deferred: AUDIO-01 (no real playback)
  - app/listen/page.js (the file being reworked — keep Suspense + useSearchParams subject handling, SECTIONS/UP_NEXT constants, theme toggle state, wave import, ALL existing Link hrefs)
  - components/TabBar.js (02-08 — render as last element)
  - .planning/phases/02-ui-design-responsive-overhaul/02-RESEARCH.md — Pattern 3 (fixed-width flex children are THE 360px bug: 212px art, 330px quote, 250px sidebar — restack or minWidth:0), Pattern 4 (TabBar in screens outside the group), anti-pattern list
  - node_modules/next/dist/docs/01-app/01-getting-started/04-linking-and-navigating.md (Suspense + useSearchParams boundary stays)
  </read_first>
  <action>
1. **Palette:** replace the inline `palette` object with `const palette = isDark ? tokens.listenDark : tokens.listenLight`; the toggle button keeps working and restyles `className="btn btn-ghost on-dark"` in dark mode (lime focus ring per §10). Page bg `palette.pageBg`.
2. **Layouts (useViewport):**
   - desktop (≥1024): the current 3-zone layout verbatim in proportions (sidebar 250 / player / sections 1.25fr + quote 330) — D-05 keeps it; all panes get `minWidth:0` hygiene.
   - tablet: sidebar 250 stays; the sections/quote row becomes a column (quote card `maxWidth:'100%'`).
   - phone: sidebar column NOT rendered at all (the global `<TabBar/>` covers Today/Subjects/Library/Pipeline; Listen is the active tab) — render instead: player card (art tile 140×140 r-lg, info stacked below/beside, wave 64px→44px tall, transport row wrapping), "UP NEXT" as a horizontal chip row under the player (three chips: title + time, surface rowBg, r-pill; NOT links — matches the Phase 1 non-link inventory), sections list full-width, quote card full-width. `<TabBar />` is the LAST element in the page tree (Pattern 4).
   - The page wrapper gets phone bottom clearance `paddingBottom:'calc(72px + env(safe-area-inset-bottom))'` (fixed bar overlays content).
3. **A11y (§11.3):** waveform container `aria-hidden="true"`; transport controls become `<button aria-label="…">` — "Play"/"Pause" (the ❚❚ button), "Skip back 15 seconds" (⟲15), "Skip forward 30 seconds" (30⟳), "Change playback speed" (1.5× chip), "Close player" (⇥) — ghost-styled, `on-dark` class in dark mode. Art tile stays a visual.
4. **Copy/tone (§14.3, honest about no-fetch):** add the empty guard `{SECTIONS.length === 0 && <ListenEmpty/>}` rendering "Nothing to listen to yet" + "Listen mode reads your study guides aloud — build one first." (§9.7 idiom) — untriggerable today, correct the moment Phase 3 data lands. Loading/error: Listen has NO fetch (static content) — leave a code comment quoting the §14.3 copy ("Cueing the episode…" / "Couldn't load the episode — check the pipeline." + Retry) for DATA-wiring to implement; do NOT add dead branches.
5. **D-11 preservation:** sidebar Links (desktop/tablet), "Read instead" → `/subjects/${encodeURIComponent(subject)}`, "Quiz me after" → `/quiz?subject=…`, dark toggle — all survive with unchanged hrefs/behavior. "Save to highlights" (quote card) becomes an honest `<button type="button" disabled className="btn btn-primary btn-undone" style={pill('primary')} title="Coming in a later update">`.
  </action>
  <verify>
    <automated>cd /Volumes/1TBFULLYPAID/Projects/yahshua/learnit && npm run build && grep -q "TabBar" app/listen/page.js && grep -q "listenDark" app/listen/page.js && grep -q "aria-hidden" app/listen/page.js && grep -q "aria-label" app/listen/page.js && grep -q "Nothing to listen to yet" app/listen/page.js && grep -q "btn-undone" app/listen/page.js && grep -q "encodeURIComponent" app/listen/page.js && echo LISTEN-OK</automated>
  </verify>
  <done>Build passes; phone shows stacked panes + TabBar, desktop proportions unchanged, toggle reads token pairs, transport buttons labeled, Phase 1 links intact.</done>
  <acceptance_criteria>
  - Viewport sweep: `node scripts/check-viewports.mjs --port 3211 --routes "/listen?subject=AI%20Agents"` — zero overflow at 360–1440 (the 212/330px fixed panes no longer blow out)
  - /tmp/shots/360-_listen_subject=AI_Agents.png: player card + UP-NEXT chip row + sections + quote stacked, bottom tab bar visible with Listen active; NO left sidebar
  - /tmp/shots/1440-_listen_subject=AI_Agents.png: 3-zone layout with sidebar (D-05 proportions kept)
  - Toggle click (playwright-core at 1024): pageBg flips between #f3f2f9 and #0e0e14 (token pairs live)
  - `curl -s "http://localhost:3211/listen?subject=Distribution" | grep -c "Distribution"` ≥ 1 and "Read instead" href renders `/subjects/Distribution` (FLOW-04 intact)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Session responsive — rail retires for TabBar on phone, LEARNER_NAME, shelf 2-col</name>
  <files>app/session/page.js</files>
  <read_first>
  - .planning/phases/02-ui-design-responsive-overhaul/02-UI-SPEC.md — §12 row 9 (Session layouts: desktop dark chrome rail 72px + light content KEPT; tablet rail stays, hero + column stack, shelf 2×2; phone rail becomes the GLOBAL bottom bar — its quiz/pathway destinations remain reachable via subject pages; hero full-width; waiting-on-you rows stack; shelf 2-col compact tiles), §14.3 (Session copy: same as Today), §7.2 (ink/inkSoft tokens for chrome, lime hero reserved use), §9.10 (lime hero)
  - .planning/phases/02-ui-design-responsive-overhaul/02-CONTEXT.md — D-03 (name single-source), D-05, D-11
  - app/session/page.js (the file being reworked — delete the local `const LEARNER_NAME = 'Sam'`; keep useQuery shelf logic, date eyebrow, hero CTAs, waiting rows, all Links)
  - lib/store.js (import LEARNER_NAME — the 02-09 export)
  - components/TabBar.js (02-08)
  </read_first>
  <action>
1. **Name:** `import { LEARNER_NAME } from '../lib/store.js'`; delete the local `'Sam'` constant. Greeting stays "Good morning, {LEARNER_NAME}" (Display 32).
2. **Layouts (useViewport):**
   - desktop: current chrome verbatim — ink page, 72px inkSoft rail (its 4 icon Links + lime logo tile + avatar keep; tokenized `var(--color-ink)`, `var(--color-ink-soft)`, `on-dark` for focus), light `var(--color-canvas)` content panel, hero 1.35fr + overnight/waiting column, shelf `repeat(4,1fr)`.
   - tablet: rail stays; hero and overnight/waiting column stack; shelf `repeat(2,1fr)`.
   - phone: rail NOT rendered — `<TabBar/>` as the last element instead (its quiz/pathway destinations remain reachable via subject pages, per the spec note — the rail Links are not lost, they are re-homed to the global bar + subject screens); hero full-width; waiting rows full-width stack; shelf `repeat(2,1fr)` compact tiles (art height 96→64). Content panel gets bottom clearance for the fixed bar.
3. **Hero (§9.10):** lime card r-2xl, ink text, "PICK UP WHERE YOU LEFT OFF" eyebrow chip (ink-on-lime per §7.1 lime rules), Display 32 title (from 34), "62% through" Caption; CTAs: "Continue reading" ink Primary Link → `/pathway/AI%20Agents` (existing href), "♪ Listen instead · 24 min" ink-on-rgba(18,18,26,.1) secondary Link → `/listen?subject=AI%20Agents`; on phone the pill pair wraps full-width.
4. **Header utilities:** "⌕ Search everything" and "+ Save a link" spans → disabled `.btn .btn-secondary .btn-undone` / `.btn .btn-primary .btn-undone` buttons with `title="Coming in a later update"` (Phase 4 actions).
5. **Waiting on you / Overnight cards:** tokenize; waiting rows keep their Links (`/subjects`, `/library`); overnight numerals Display 32 lh 1, failed numeral `var(--color-danger-bright)`, "Open the run log →" keeps accent `.btn .btn-link` → /pipeline.
6. **States (§14.3 — same as Today):** loading → shelf skeleton tiles + sr-only "Setting up your session…" while subjects load; error → "Couldn't load your morning — check the pipeline." + Retry `role="alert"` (add `res.ok` throw + isError/refetch to the query); empty → same copy as Today's empty.
  </action>
  <verify>
    <automated>cd /Volumes/1TBFULLYPAID/Projects/yahshua/learnit && npm run build && grep -q "TabBar" app/session/page.js && grep -q "LEARNER_NAME" app/session/page.js && ! grep -q "'Sam'" app/session/page.js && grep -q "useViewport" app/session/page.js && grep -q "Setting up your session" app/session/page.js && grep -q "btn-undone" app/session/page.js && grep -q "encodeURIComponent" app/session/page.js && echo SESSION-OK</automated>
  </verify>
  <done>Build passes; phone Session shows TabBar not rail, name is Iven, shelf 2-col; desktop chrome unchanged; states and honest buttons land.</done>
  <acceptance_criteria>
  - `! grep -rn "Sam" app/` — the split is dead everywhere
  - Viewport sweep: `node scripts/check-viewports.mjs --port 3211 --routes /session` — zero overflow at 360–1440
  - /tmp/shots/360-session.png: full-width lime hero, stacked waiting rows, 2-col shelf, bottom tab bar; NO left rail
  - /tmp/shots/1024-session.png: 72px rail + 4-across shelf (D-05 desktop kept)
  - `curl -s http://localhost:3211/session | grep -c "pathway/AI%20Agents"` ≥ 1 (hero CTA href survives — D-11)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL query ?subject= → Listen/Session render | client query rendered as text and into encoded hrefs (existing discipline) |
| browser → GET /api/subjects (Session shelf) | read-only; this plan adds the standard !res.ok throw + error branch |
| client theme state → Listen render | local toggle, no persistence, no server surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11-01 | Tampering (XSS) | subject/section title rendering | accept | JSX text; keep auto-escaping; no innerHTML |
| T-02-11-02 | Elevation | disabled Search/Save/Save-to-highlights buttons | accept | Disabled without handlers; Phase 4 wires |
| T-02-11-03 | Tampering | tokens.js additive change | accept | Constants only; import-safety re-proven by the 02-01 import test in Task 0's verify |
| T-02-11-04 | DoS | 96-bar waveform render on phone | accept | Static divs, no animation (reduced-motion covered globally); negligible cost |
</threat_model>

<verification>
- `npm run build` passes.
- Viewport sweep (port 3211): `/listen?subject=AI%20Agents` and `/session` clean at all five widths — the two worst 360px offenders fixed.
- D-11: Listen sidebar/Read instead/Quiz me after + Session rail (desktop)/hero CTAs/waiting rows all still Links with encoded hrefs; ?subject= context flows in and out.
- Toggle parity: Listen dark mode renders identical contrast story via token pairs (spot-check on-dark focus ring visible when tabbing the toggle in dark mode).
</verification>

<success_criteria>
- Phone: both screens are single-column, thumb-navigable, and end in the global tab bar — the app chrome finally feels native at 360px.
- Desktop: nothing about these two distinctive screens was flattened (D-05 proportions kept).
- The app greets one person, consistently, by name.
</success_criteria>

<output>
After completion, create `.planning/phases/02-ui-design-responsive-overhaul/02-11-SUMMARY.md`
</output>
