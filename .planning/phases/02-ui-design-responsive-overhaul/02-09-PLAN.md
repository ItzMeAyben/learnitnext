---
phase: 02-ui-design-responsive-overhaul
plan: 09
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified:
  - components/Today.js
  - lib/store.js
autonomous: true
requirements: [UI-02, UI-03, UI-04, UI-05]
must_haves:
  truths:
    - "An onboarded user opening / sees a welcoming hierarchy: (1) lime greeting hero 'Built while you slept, Iven' with one-line overnight status and a subtle streak chip, (2) ONE primary action — ink 'Continue reading' into the most recent subject's active pathway stage — plus a Ghost 'Listen instead', (3) subjects grid, (4) a visually quieter demoted rail (now playing, saved yesterday, last night's run) — no wall of equally-weighted widgets (D-03)"
    - "The learner's name is 'Iven' on BOTH Today and Session, imported from the single lib/store.js LEARNER_NAME export (the Iven/Sam split ends)"
    - "Today is fully usable at 360px: hero → subjects 1-col → fresh material 2-col → saved yesterday → run health → now playing, header actions as a wrapping row of pills — no horizontal scroll"
    - "Loading shows hero + column skeletons with sr-only 'Waking up your dashboard…'; errors show 'Couldn't load your morning — check the pipeline.' + Retry (Today previously had NO error branch — this adds it); empty shows 'Your first night hasn't run yet'"
    - "Every Phase 1 wire survives: Finance tile → /subjects, all four fresh-material Links, Session view, 'See the full run →', subject tiles with encoded hrefs"
  artifacts:
    - path: "lib/store.js"
      provides: "export const LEARNER_NAME = 'Iven' — single source for the learner's name (D-03)"
      contains: "LEARNER_NAME"
    - path: "components/Today.js"
      provides: "Redesigned Today hub per §14.1 with responsive §12 row 3 layouts and §14.3 states"
      contains: "Built while you slept"
  key_links:
    - from: "components/Today.js"
      to: "/pathway/AI%20Agents#active-stage"
      via: "hero 'Continue reading' Link into the featured subject's active stage (existing #active-stage anchor from 02-04)"
      pattern: "active-stage"
    - from: "components/Today.js"
      to: "lib/store.js"
      via: "import { LEARNER_NAME }"
      pattern: "LEARNER_NAME"
    - from: "app/session/page.js"
      to: "lib/store.js"
      via: "consumed in 02-11 (this plan only creates the export)"
      pattern: "LEARNER_NAME"
---

<objective>
Redesign Today as the welcoming hub D-03 locks: greeting hero with one-line overnight status, exactly ONE primary next action ("Continue reading" into the most recent subject's active pathway stage), subjects grid, and quietly demoted secondary info — plus the responsive §12 row 3 layouts and the §14.3 tone states (including Today's missing error branch). Ends the Iven/Sam name split by exporting `LEARNER_NAME` from `lib/store.js`.

Purpose: UI-02 (the dashboard requirement — clear hero, single obvious next action), UI-03, UI-04, UI-05. D-11 fence: every Phase 1 Link on this screen survives (Finance tile, fresh material, Session view, run link, subject tiles).

Output: reworked `components/Today.js` (moved here from app/(dashboard)/page.js by 02-02) and a one-line addition to `lib/store.js`. Widget DATA stays mock (DATA-04 is Phase 3 — this is design hierarchy, not data wiring).
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ui-design-responsive-overhaul/02-CONTEXT.md
@.planning/phases/02-ui-design-responsive-overhaul/02-RESEARCH.md
@components/Today.js
@lib/store.js

<interfaces>
From lib/tokens.js (created in 02-01):
```js
export const tokens            // color/space/radius/type/status
export function pill(variant)       // layout-only; className="btn btn-<variant>"
export function card(opts)          // {dark:true} for the Now playing ink card
export function statusPill(status)
export const GLYPHS
export function useViewport()       // phone column-stack vs desktop grid+rail
```
CSS classes (globals.css from 02-01): `.btn .btn-primary .btn-secondary .btn-ghost .btn-link .btn-undone`, `.skeleton`.
OWNERSHIP RULE: no inline background/color on .btn-* carriers.
lime-card idioms (§9.1/§9.10): Primary stays ink-on-lime; secondary pills on lime use ink on rgba(18,18,26,.1); chips on lime use rgba(18,18,26,.1) bg.
Today renders inside <AppShell> (02-08) — do NOT add page chrome, TopNav, TabBar, or page background here; the component starts at the hero.
lib/store.js addition: `export const LEARNER_NAME = 'Iven'` (value from the current Today copy — Session's local 'Sam' is the bug D-03 ends; 02-11 consumes the export).
Dev-server protocol: scratch port 3199; `node scripts/check-viewports.mjs --port 3199 --routes /` (cookie fixture models the onboarded user — Today only renders with the cookie).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: LEARNER_NAME export + the §14.1 hierarchy (hero, ONE primary, subjects, demoted rail)</name>
  <files>lib/store.js, components/Today.js</files>
  <read_first>
  - .planning/phases/02-ui-design-responsive-overhaul/02-UI-SPEC.md — §14.1 (Today structure in order: greeting hero copy 'Built while you slept, {name}' + 'Last run 6:04am · 6 new sources read · 3 subjects updated' + streak chip '37-day streak · keep it going'; ONE primary 'Continue reading' + Ghost '♪ Listen instead · 24 min'; subjects grid featured+list; demoted rail Now playing/Saved yesterday/Last night's run as compact Caption-headed cards; fresh material strip between hero and subjects), §12 row 3 (three-width layouts incl. phone order), §9.10 (lime hero), §6 (Display 32 h1 + lh-1 stat numerals — streak '37' becomes 32)
  - .planning/phases/02-ui-design-responsive-overhaul/02-CONTEXT.md — D-03 (locked hierarchy), D-11
  - components/Today.js (the current moved-verbatim screen — reuse its useQuery hooks, featured/others logic, savedRows derivation, FRESH_MATERIAL array, waveBars)
  - lib/store.js (add ONE export; touch nothing else)
  - .planning/phases/02-ui-design-responsive-overhaul/02-RESEARCH.md — UI-02 row (LEARNER_NAME verification: Today hardcodes Iven, Session defines Sam)
  </read_first>
  <action>
1. **lib/store.js:** add `export const LEARNER_NAME = 'Iven'` near the top (after the header comment). Nothing else changes.
2. **Hero (§14.1.1):** lime card (`tokens.color.lime`, r-2xl, padding xl/md-phone): h1 Display 32 ink — `Built while you slept, {LEARNER_NAME}` (import from `'../lib/store.js'`; the old gray/ink two-tone collapses to single ink Display — name emphasis comes free at 700); overnight caption Body ink at rgba(18,18,26,.65): `Last run {pipeline?.lastRunAt ?? '6:04am'} · 6 new sources read · 3 subjects updated`; streak chip Caption 700 on rgba(18,18,26,.1) pill: `37-day streak · keep it going`. The big Streak card from the old right rail is DELETED (streak now lives only in this chip).
3. **Header actions row (below/behind the hero per width):** ONE primary — `<Link href={`/pathway/${encodeURIComponent(featured.id)}#active-stage`} className="btn btn-primary" style={pill('primary')}>Continue reading</Link>` (featured = the existing AI Agents featured logic; the hash lands on 02-04's active-stage anchor and natively scrolls, reduced-motion safe). Beside it Ghost `<Link href={`/listen?subject=${encodeURIComponent(featured.id)}`} className="btn btn-ghost" style={pill('ghost')}>♪ Listen instead · 24 min</Link>`. Then the demoted utilities: "Session view" stays a Link → `.btn .btn-secondary`; "Save a link" and "Run now" become disabled `<button className="btn btn-ghost btn-undone" title="Coming in a later update">` (Phase 4 actions — Run now is NOT wired here, CAPT-02 fence). On phone this row wraps to full-width pills in order: Continue reading (primary first), Listen instead, Session view, Save a link, Run now (§12 row 3's "2-up Ghost row" absorbs the utilities).
4. **Fresh material strip:** the existing FRESH_MATERIAL card row (all four Links UNCHANGED hrefs — D-11) moves between hero and subjects; grid `repeat(auto-fit, minmax(160px, 1fr))` → 4-across desktop / 2-col phone. Icon tiles keep their tinted idiom tokenized.
5. **Subjects grid:** featured colored tile (store color, amber progress, "New today" amber chip) + list tiles for others + the Finance tile (Link → `/subjects`, meta "3 sources · needs 2 more") — ALL existing hrefs preserved. Grid: desktop `1fr 1fr` inside the left column of the main grid (see 6), phone 1-col.
6. **Layout shell:** desktop `gridTemplateColumns:'1fr 336px'` — left column: fresh strip + subjects card; right column (the DEMOTED rail, visually lighter: Caption-12 headers `NOW PLAYING`/`SAVED YESTERDAY`/`LAST NIGHT'S RUN` uppercase, compact cards, no big numerals): Now playing ink mini-card (`card({dark:true})`, 34px wave via existing waveBars, lime art tile, transport glyphs decorative), Saved yesterday card (first 3 savedRows + `Library →` Link `.btn .btn-link` — change slice(0,4) to slice(0,3) per §14.1), Last night's run card (4-segment mini bar + `See the full run →` `.btn .btn-link` → /pipeline). Tablet: rail becomes a 2-col row below subjects. Phone (useViewport): everything stacks in §12 row 3 order — hero → action row → subjects → fresh 2-col → saved yesterday → run health → now playing. No fixed-width children without minWidth:0.
7. Status pills → `statusPill()`; all hex → tokens; STATUS_PILL local map DELETED in favor of the shared factory (D-07 dedup).
  </action>
  <verify>
    <automated>cd /Volumes/1TBFULLYPAID/Projects/yahshua/learnit && npm run build && grep -q "LEARNER_NAME" lib/store.js && grep -q "LEARNER_NAME" components/Today.js && grep -q "Built while you slept" components/Today.js && grep -q "Continue reading" components/Today.js && grep -q "active-stage" components/Today.js && grep -q "37-day streak" components/Today.js && grep -q "btn-undone" components/Today.js && ! grep -q "const STATUS_PILL" components/Today.js && echo TODAY-HIERARCHY-OK</automated>
  </verify>
  <done>Build passes; hero/one-primary/subjects/demoted-rail hierarchy renders at all widths; name comes from the store export; Save/Run are honest disabled pills.</done>
  <acceptance_criteria>
  - `grep -c "encodeURIComponent" components/Today.js` ≥ 6 (subject tiles, fresh material, continue, listen)
  - `grep -q 'href="/subjects"' components/Today.js` (Finance tile Link survives — D-11/FLOW-05)
  - Exactly ONE `.btn-primary` above the fold: `grep -c "btn-primary" components/Today.js` counts Continue reading (+ none among utilities — Save/Run are btn-ghost btn-undone)
  - Cookie-fixture capture /tmp/shots/360-_.png (via check-viewports on /) shows the phone stack order hero→actions→subjects
  - /tmp/shots/1024-_.png shows hero full-width, subjects left, 320px-ish rail right, NO giant Streak card
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: §14.3 tone states — Today gains loading, error, and empty branches</name>
  <files>components/Today.js</files>
  <read_first>
  - .planning/phases/02-ui-design-responsive-overhaul/02-UI-SPEC.md — §14.3 Today row (loading 'Waking up your dashboard…'; empty 'Your first night hasn't run yet' + 'Save a link, and tomorrow this page fills in.' + Ghost 'Start setup'; error 'Couldn't load your morning — check the pipeline.' + Retry), §9.6 (skeleton shapes: hero + 3 column skeletons), §9.8 (error idiom, role=alert), §9.7 (empty idiom)
  - .planning/phases/01-connect-screens/01-VERIFICATION.md — anti-pattern row noting Home's fetchSubjects lacks res.ok throw and has no error branch (IN-01) — this task closes it
  - components/Today.js (after Task 1)
  </read_first>
  <action>
1. **Fetch hardening:** all three fetch helpers gain `if (!res.ok) throw new Error(`HTTP ${res.status}`)` (mirrors the other screens' Phase 1 WR-01 fix); destructure `isError`/`refetch`/`isLoading` from the subjects query (the screen's primary query).
2. **Loading (isLoading, replaces the current implicit blank):** hero-shaped `.skeleton` (a 120px r-2xl block) + a 3-column skeleton grid (3 × 200px-tall `.skeleton` cards in the subjects/rail shape), container `aria-busy="true"` + sr-only "Waking up your dashboard…".
3. **Error (isError):** `role="alert"` Body danger-text "Couldn't load your morning — check the pipeline." + `<button className="btn btn-primary" style={pill('primary')} onClick={() => refetch()}>Retry</button>` (§14.3 verbatim).
4. **Empty (`subjects && subjects.length === 0`):** §9.7 idiom — "Your first night hasn't run yet" (Heading 20) + "Save a link, and tomorrow this page fills in." (Body secondary) + Ghost `Link href="/onboarding" className="btn btn-ghost" style={pill('ghost')}` "Start setup" (§14.3). Render inside the hero card slot so the greeting still greets.
5. These branches wrap the Task 1 layout; the happy path is unchanged.
  </action>
  <verify>
    <automated>cd /Volumes/1TBFULLYPAID/Projects/yahshua/learnit && npm run build && grep -q "Waking up your dashboard" components/Today.js && grep -q "Couldn't load your morning" components/Today.js && grep -q "Your first night hasn't run yet" components/Today.js && grep -q "role=\"alert\"" components/Today.js && grep -q "res.ok" components/Today.js && grep -q "className=\"skeleton\"" components/Today.js && echo TODAY-STATES-OK</automated>
  </verify>
  <done>Build passes; Today renders §14.3 skeleton/empty/error copy verbatim; fetch helpers throw on !res.ok.</done>
  <acceptance_criteria>
  - `grep -c "res.ok" components/Today.js` ≥ 3 (all three helpers hardened)
  - Error branch provable: temporarily renaming /api/subjects is NOT needed — the branch is grep-verified and the pattern matches the proven Subjects implementation
  - Viewport sweep (cookie fixture): `/` clean at all five widths with the new hierarchy
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → GET /api/subjects, /api/library, /api/pipeline | read-only fetches; this plan adds the standard !res.ok throws (hardening, not new surface) |
| cookie learnit_onboarded → whether Today renders at all | decided upstream in app/page.js (02-02); Today itself reads no cookies |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09-01 | Tampering (XSS) | FRESH_MATERIAL/savedRows/pipeline rendering | accept | JSX text only; keep dangerouslySetInnerHTML absent |
| T-02-09-02 | Elevation | disabled Save-a-link / Run-now buttons | accept | Disabled, no onClick — CAPT-01/02 wire them in Phase 4; title tooltip explains |
| T-02-09-03 | Tampering | LEARNER_NAME export | accept | Compile-time constant, not user data; ends a copy drift bug |
| T-02-09-04 | Information disclosure | overnight-status caption | accept | Static/mock store values; no user PII exists in the app |
</threat_model>

<verification>
- `npm run build` passes.
- Cookie-fixture viewport sweep (port 3199): `/` clean at 360/390/768/1024/1440.
- Hierarchy eyeball: 1024 capture shows hero → fresh strip → subjects+rail; ONE ink primary. 360 capture shows the §12 row 3 phone order.
- D-11: Finance tile, 4 fresh-material Links, Session view, See-the-full-run, subject tiles all still Links with encoded hrefs.
</verification>

<success_criteria>
- Today answers "what should I do next?" with one ink button and a warm greeting — UI-02 delivered.
- The rail whispers; the subjects sing; the streak is a chip, not a monument.
- Session and Today agree on the user's name (after 02-11 consumes the export).
</success_criteria>

<output>
After completion, create `.planning/phases/02-ui-design-responsive-overhaul/02-09-SUMMARY.md`
</output>
