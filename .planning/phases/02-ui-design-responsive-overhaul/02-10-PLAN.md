---
phase: 02-ui-design-responsive-overhaul
plan: 10
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified:
  - app/onboarding/page.js
autonomous: true
requirements: [UI-03, UI-04, UI-05]
must_haves:
  truths:
    - "Onboarding keeps its five steps and Skip with identical FLOW-02 semantics (Back clamps at 1, Continue advances to 5, Finish completes, both exits set the flag via completeOnboarding) — zero behavior change"
    - "The wizard feels guided at every width: a progress indicator shows done-checks (green), current (accent), and upcoming (outline) — the sidebar stepper on desktop, a horizontal dot bar on tablet, dots + 'Step X of 5' caption on phone (D-10)"
    - "Every control is a semantic button or Link ≥44px: Back (Ghost, 55% opacity at step 1), Continue (Primary), 'Set up later' (Ghost), 'Skip setup →' (.btn-link), Finish setup (Primary)"
    - "Step 3's token field is a real labeled input: <label>BotFather token</label> + monospace <input>, Verify is a <button>"
    - "Onboarding is fully usable at 360px: door cards 1-column, footer buttons stacked full-width (Back above Continue), no horizontal scroll, no dead ends"
  artifacts:
    - path: "app/onboarding/page.js"
      provides: "Guided, responsive, token-driven onboarding wizard (D-10) with real form semantics"
      contains: "BotFather token"
  key_links:
    - from: "app/onboarding/page.js"
      to: "app/actions.js"
      via: "existing finish() helper calling completeOnboarding() (02-02 wiring — preserved exactly)"
      pattern: "completeOnboarding"
    - from: "app/onboarding/page.js"
      to: "lib/tokens.js"
      via: "tokens/pill/useViewport for layout + stepper variants"
      pattern: "tokens\\.js"
---

<objective>
Give onboarding its guided feel and responsive layouts (D-10/D-04): the sidebar stepper becomes the progress indicator that reflows per width (sidebar → horizontal labeled bar → dots + caption), every clickable span becomes a semantic button/Link, step 3's token field becomes a real labeled input, and the whole flow tokenizes — all while FLOW-02 behavior stays byte-identical and the 02-02 Server Action wiring (Finish/Skip → completeOnboarding) is preserved untouched.

Purpose: UI-04 (guided, flash-free, no-dead-ends onboarding is the requirement's named screen) + UI-03/UI-05. This is the last screen in the app with onClick spans as primary controls — ending that is D-11 hygiene, not a behavior change.

Output: rewritten `app/onboarding/page.js`. No new steps, no gate logic, no copy-story changes (STEP_META titles/bodies and the encouraging meta lines stay verbatim).
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ui-design-responsive-overhaul/02-CONTEXT.md
@.planning/phases/02-ui-design-responsive-overhaul/02-RESEARCH.md
@app/onboarding/page.js

<interfaces>
From lib/tokens.js (created in 02-01):
```js
export const tokens       // color/space/radius/type; color.success #22c55e, color.accent #6c3ce9
export function pill(variant)  // layout-only; className="btn btn-<variant>"
export function card(opts)
export const GLYPHS        // play '▶', telegram '✈', browser '◫', text '✉'
export function useViewport() // 'phone' | 'tablet' | 'desktop' — picks stepper layout + footer stacking
```
CSS classes (globals.css from 02-01): `.btn .btn-primary .btn-secondary .btn-ghost .btn-link .btn-undone`, `.skeleton`, `.only-phone .only-tablet .only-desktop`.
OWNERSHIP RULE: no inline background/color on .btn-* carriers.
02-02 wiring to preserve EXACTLY: `import { completeOnboarding } from '../actions.js'`; the async `finish()` helper (localStorage try/catch + `await completeOnboarding()`); "Skip setup →" and "Finish setup" keep `href="/"` no-JS fallback with onClick={finish}.
Dev-server protocol: scratch port 3201; `node scripts/check-viewports.mjs --port 3201 --routes /onboarding --no-cookie` (onboarding needs no cookie; fresh profile is the real first-run state).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Responsive wizard layout — stepper reflow, phone stacking, tokenization</name>
  <files>app/onboarding/page.js</files>
  <read_first>
  - .planning/phases/02-ui-design-responsive-overhaul/02-UI-SPEC.md — §12 row 2 (onboarding layouts: desktop sidebar 300px + panel side-by-side (current proportions kept); tablet sidebar collapses to a horizontal step-progress bar (5 dots + labels) above the panel, cost card hidden; phone dots-only + 'Step X of 5' caption, door cards 1-col, footer buttons full-width stacked Back-ghost-above-Continue-primary), §14.4 (guided feel: done-checks success green, current accent, upcoming outline; sidebar stepper RESTYLED not replaced; encouraging meta lines stay; Back disabled-ish at step 1 at 55% opacity; Skip visible every step), §9.4 sidebar-nav-item idiom
  - .planning/phases/02-ui-design-responsive-overhaul/02-CONTEXT.md — D-10 (locked), D-04, D-11 (FLOW-02 must not regress)
  - app/onboarding/page.js (the file being rewritten — keep STEPS, STEP_META, useState step/telegramConnected, InfoCard, all copy strings, the finish() wiring from 02-02)
  - node_modules/next/dist/docs/01-app/01-getting-started/07-mutating-data.md (this page invokes the Server Action completeOnboarding from event handlers — the documented client-invocation pattern the 02-02 wiring follows and this plan must preserve)
  - .planning/phases/02-ui-design-responsive-overhaul/02-RESEARCH.md — Pattern 3 rung 4 (stepper layout swap is structural), Pitfall 7 (wizard semantics regression)
  </read_first>
  <action>
1. **Stepper component (in-file):** extract the step list into a `Stepper({ step })` render used in three layouts via `useViewport()`:
   - desktop: the existing 300px sidebar column — rows restyled per §14.4: done = white row + success-green ✓ circle; active = accent bg row (white text, numbered circle on rgba-white); upcoming = outline circle (`1.5px solid var(--color-border-strong)`, `var(--color-text-faint)` number) + text-secondary label. Keep the "Set it up once" intro block and the lime Cost card in the sidebar.
   - tablet: sidebar column hidden EXCEPT a horizontal bar above the main panel: five dot+label items in a row (labels Caption 12; done ✓ green, current accent-filled, upcoming outline), Cost card hidden.
   - phone: dots-only row + a Caption "Step {step} of 5" (this caption also already exists in the footer meta — keep one, drop the duplicate).
2. **Main panel:** keep the white r-2xl panel, eyebrow `STEP {step} OF 5` (Caption 12/700/+0.6 uppercase), title Heading 20→ actually the panel titles are the page's h1-per-step — keep ONE h1 on the panel title at Display 32 (absorbing current 26). Door grid (step 3): `repeat(auto-fit, minmax(260px, 1fr))` — 2×2 desktop/tablet, 1-col phone (§12 row 2). The playlist-warning callout tokenizes (amber-tint tile + amber-text glyph).
3. **Step 3 Telegram card:** replace the fake mono div with a real form field: `<label htmlFor="bot-token" style={captionLabel}>BotFather token</label>` + `<input id="bot-token" type="text" placeholder="paste your BotFather token" style={{ fontFamily: tokens.type.mono, fontSize: 12, …tokenized field styles }} />` (§11.4); "Verify" becomes `<button type="button" className="btn btn-primary" style={pill('primary')} onClick={() => setTelegramConnected(true)}>Verify</button>`; keep the Connected/Waiting pill logic. The mono field keeps `overflow-x: auto` internally (§10 zoom exception for mono token fields).
4. **Footer controls:** Back → `<button type="button" className="btn btn-ghost" style={pill('ghost')} disabled={step === 1} onClick={() => setStep((s) => Math.max(1, s - 1))}>Back</button>` — the disabled state gets the class's `opacity:.55` (§14.4's "Ghost at 55% opacity" — exactly what .btn:disabled does; delete the inline opacity). Continue/CTA → `<button type="button" className="btn btn-primary" style={pill('primary')} onClick={() => setStep((s) => Math.min(5, s + 1))}>{STEP_META[step].cta}</button>`. "Skip setup →" keeps its Link + finish() wiring, restyled `className="btn btn-link"`. "Finish setup" keeps its Link + finish() wiring, restyled `className="btn btn-primary" style={pill('primary')}`. Both "Set up later" pills → `<button type="button" className="btn btn-ghost" style={pill('ghost')}>`. Footer meta line keeps the encouraging copy verbatim ("2 of 4 doors open · that's enough to start" / "3 of 4 doors open · nice pace" / "Step {n} of 5").
5. **Phone footer:** buttons stack full-width in order Back (ghost) above Continue (primary) via `useViewport()==='phone'` flexDirection column, each `width:'100%'`; Skip stays inline in the meta row above (§12 row 2).
6. Tokenize the page: canvas `var(--color-canvas)`, panel `card()`, InfoCard borders `var(--color-border)`, lime cost card `var(--color-lime)`, all grays → the §7.2 text hierarchy (secondary for body — the #8b889f-on-white small text moves to `var(--color-text-secondary)` per §7.6).
  </action>
  <verify>
    <automated>cd /Volumes/1TBFULLYPAID/Projects/yahshua/learnit && npm run build && grep -q "htmlFor=\"bot-token\"" app/onboarding/page.js && grep -q "BotFather token" app/onboarding/page.js && grep -q "<input" app/onboarding/page.js && grep -q "completeOnboarding" app/onboarding/page.js && grep -q "btn-primary" app/onboarding/page.js && grep -q "btn-ghost" app/onboarding/page.js && grep -q "btn-link" app/onboarding/page.js && grep -q "useViewport" app/onboarding/page.js && grep -c "onClick" app/onboarding/page.js && echo ONBOARDING-LAYOUT-OK</automated>
  </verify>
  <done>Build passes; stepper reflows per width; token field is a labeled input; footer controls are semantic pills; finish/skip wiring untouched.</done>
  <acceptance_criteria>
  - `grep -c "<span[^>]*onClick\|<div[^>]*onClick" app/onboarding/page.js` returns 0 — zero clickable spans/divs remain
  - `grep -q "disabled={step === 1}" app/onboarding/page.js` — Back's guard is attribute-driven (55% via class)
  - Wizard traversal (port 3201, playwright-core or manual, fresh profile): Back at step 1 inert; Continue 1→5; Finish setup lands on / with the cookie set (FLOW-02 + D-01 round-trip); Skip setup → same from any step; Verify flips the pill
  - Viewport sweep: `node scripts/check-viewports.mjs --port 3201 --routes /onboarding --no-cookie` — no overflow at any width
  - /tmp/shots/360-onboarding.png shows dots + caption, 1-col door cards, stacked Back/Continue
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: FLOW-02 regression fence for onboarding (D-10/D-11)</name>
  <files>app/onboarding/page.js</files>
  <read_first>
  - .planning/phases/01-connect-screens/01-VERIFICATION.md — truths 2 and 3 (finish/skip land on Today with flag set; Back/Continue walk 1↔5; Set-up-later pills advance) — the exact behaviors this fence re-proves
  - .planning/phases/02-ui-design-responsive-overhaul/02-UI-SPEC.md — §15 (regression fence; FLOW-01 amendment context), §14.4 ("FLOW-02 semantics unchanged")
  - app/onboarding/page.js (after Task 1)
  </read_first>
  <action>
No new code — a verification pass that must pass before the plan closes (run against the port-3201 dev server):

1. **State machine parity:** confirm `useState(1)`, `Math.max(1, s-1)`, `Math.min(5, s+1)` and `STEP_META[step].cta` labels for steps 1–5 are unchanged; both "Set up later" handlers still advance; "Verify" still sets telegramConnected (grep + click).
2. **Exit parity:** both exits call `finish()` (localStorage try/catch + `await completeOnboarding()`); after Finish, `context.cookies()` (playwright-core) contains `learnit_onboarded=1` AND the browser sits on `/` rendering Today; after Skip (fresh context), same.
3. **No-JS fallback:** both exit Links keep `href="/"` (curl the SSR HTML: `curl -s http://localhost:3201/onboarding | grep -c 'href="/"'` ≥ 2).
4. **Storage-unavailable path:** the try/catch around localStorage remains (§14.3 onboarding row: silent, navigation still works).
5. Fix anything the pass surfaces (the likely failure mode: a restyled control lost its onClick/href during Task 1's span→button conversion).
  </action>
  <verify>
    <automated>cd /Volumes/1TBFULLYPAID/Projects/yahshua/learnit && npm run build && grep -c "Math.max(1" app/onboarding/page.js && grep -c "Math.min(5" app/onboarding/page.js && grep -c "finish()" app/onboarding/page.js && grep -q "localStorage.setItem" app/onboarding/page.js && echo FLOW02-FENCE-OK</automated>
  </verify>
  <done>All five FLOW-02 checks pass on the redesigned wizard; the fence is recorded in the plan SUMMARY with the click-trace evidence.</done>
  <acceptance_criteria>
  - Playwright-core click-trace at 360 AND 1024: step transitions 1→2→3→4→5→(Finish)→`/` + cookie; 5→4→3 (Back); step-3 Set up later → step 4; Verify flips the pill
  - `curl -s http://localhost:3201/onboarding | grep -o 'href="/"' | wc -l` ≥ 2 (both no-JS fallbacks present in SSR HTML)
  - Zero hydration errors in the dev-server log during the walkthrough
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Server Action completeOnboarding (app/actions.js) | invoked from Finish/Skip handlers — pre-existing 02-02 surface, unchanged here |
| localStorage → try/catch | best-effort compat flag; failure is silent by design |
| token input → client state | the BotFather token field is MOCK (no network); whatever is typed never leaves the component (Phase 4 concern) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10-01 | Tampering | token input value | accept | Pure client mock — never submitted, never stored, never rendered back except as React-escaped value; note for Phase 4: real verification must POST to a server-held secret, never embed one |
| T-02-10-02 | Elevation | completeOnboarding direct POST | accept | Same disposition as 02-02 (non-sensitive self-service cookie); this plan does not widen it |
| T-02-10-03 | Tampering (XSS) | STEP_META copy rendering | accept | Static JSX literals; auto-escaped |
| T-02-10-04 | Repudiation | — | accept | No auth/audit surface (single-user tool, unchanged) |
</threat_model>

<verification>
- `npm run build` passes.
- Viewport sweep (port 3201, --no-cookie): `/onboarding` clean at 360–1440.
- FLOW-02 fence (Task 2) passes at phone and desktop widths.
- D-01 round-trip intact: Finish/Skip → cookie → `/` renders Today (amended FLOW-01 still satisfied end-to-end).
</verification>

<success_criteria>
- The wizard reads as a guided 5-step path on every device — progress always visible, never a dead end.
- Not one onClick span remains; keyboard and screen-reader users get real buttons, a real label, and real link fallbacks.
- Phase 1's most-traveled flow (it is the front door) works exactly as verified in 01-VERIFICATION.
</success_criteria>

<output>
After completion, create `.planning/phases/02-ui-design-responsive-overhaul/02-10-SUMMARY.md`
</output>
