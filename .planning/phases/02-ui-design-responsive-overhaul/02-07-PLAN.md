---
phase: 02-ui-design-responsive-overhaul
plan: 07
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified:
  - app/(dashboard)/pipeline/page.js
autonomous: true
requirements: [UI-03, UI-04, UI-05]
must_haves:
  truths:
    - "The Pipeline screen is fully usable at 360px: step cards in a 2-across grid, the status ladder as a vertical stack with ↓ arrows, banners full-width, and the run log full-width below — no horizontal scroll"
    - "'Run all now' still triggers the existing POST /api/pipeline/run mutation and the UI updates when it resolves (Phase 1 behavior preserved exactly)"
    - "Loading shows 6 step-card skeletons with sr-only 'Replaying last night's run…'; errors show 'Couldn't load the pipeline.' + a Retry button with role=alert"
    - "An empty pipeline state renders 'No runs yet' with the Run-all pill still present (§14.3)"
    - "Non-functional controls (Schedule ▾, Copy log, Open skill, Rename, Keep, Retry all) are honest disabled .btn-undone buttons — Retry-all in the danger variant"
  artifacts:
    - path: "app/(dashboard)/pipeline/page.js"
      provides: "Responsive pipeline: reflowing steps/ladder/log, tokenized run log, §14.3 states"
      contains: "Replaying last night's run"
  key_links:
    - from: "app/(dashboard)/pipeline/page.js"
      to: "/api/pipeline/run"
      via: "existing useMutation POST (must survive the rewrite untouched)"
      pattern: "pipeline/run"
    - from: "app/(dashboard)/pipeline/page.js"
      to: "lib/tokens.js"
      via: "tokens/card/statusPill/useViewport"
      pattern: "tokens\\.js"
---

<objective>
Make the Pipeline screen responsive (D-04/D-05: 6-across step cards reflow to 3-across tablet and 2-across phone; the → arrow status ladder wraps 2×2 on tablet and becomes a ↓ vertical stack on phone; the 400px run log drops below full-width) and land its §14.3 tone states — while preserving the one live mutation in the app ("Run all now") byte-for-byte.

Purpose: UI-03 (360px), UI-04 (pipeline loading/empty/error copy), UI-05 (mono run log and ink cards tokenized per §6/§9.2). D-11 fence: Run-all keeps working; no dead spans introduced.

Output: rewritten `app/(dashboard)/pipeline/page.js`. Step/log data stays mock (Phase 5 owns semantics).
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ui-design-responsive-overhaul/02-CONTEXT.md
@.planning/phases/02-ui-design-responsive-overhaul/02-RESEARCH.md
@app/(dashboard)/pipeline/page.js

<interfaces>
From lib/tokens.js (created in 02-01):
```js
export const tokens          // color/space/radius/type/status; type.mono = "'JetBrains Mono',ui-monospace,monospace"
export function pill(variant)     // layout-only; className="btn btn-<variant>"
export function card(opts)        // {dark:true} → ink card (run log, §9.2 dark variant)
export function statusPill(status)
export function useViewport()     // 'phone' drives the ladder axis + log placement swap
```
CSS classes (globals.css from 02-01): `.btn .btn-primary .btn-secondary .btn-ghost .btn-danger .btn-undone`, `.skeleton`, `.on-dark` (lime focus ring for buttons inside the ink run-log card).
OWNERSHIP RULE: no inline background/color on .btn-carriers; buttons inside the dark run-log card get `className="btn btn-ghost on-dark"` so the focus ring swaps to lime (§10).
Dev-server protocol: scratch port 3197; `node scripts/check-viewports.mjs --port 3197 --routes /pipeline`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Header, step cards, tone states — keep the Run-all mutation alive</name>
  <files>app/(dashboard)/pipeline/page.js</files>
  <read_first>
  - .planning/phases/02-ui-design-responsive-overhaul/02-UI-SPEC.md — §12 row 11 (steps 6-across → 3-across ×2 tablet → 2-across ×3 phone; ladder wraps/stacks; banners full-width; log full-width below), §14.3 (pipeline loading/empty/error copy), §9.1 (Run all now = Primary — the ONE primary per view; not-yet-wired pills disabled), §6 (Display 32 h1 + stat numerals, mono Caption for run log), §9.6 (skeleton)
  - .planning/phases/02-ui-design-responsive-overhaul/02-CONTEXT.md — D-04, D-09; deferred: PIPE-01/02 semantics (do NOT touch status logic)
  - app/(dashboard)/pipeline/page.js (the file being rewritten — keep useQuery ['pipeline'], useMutation POST /api/pipeline/run + onSuccess setQueryData, StepCard/RunLogLine structure, okCount derivation)
  </read_first>
  <action>
Rewrite chrome, steps, and states:

1. **Header:** h1 "Pipeline" Display 32; sub-line copy kept ("Six skills, daily at 6:00am · last run finished {lastRunAt} in 4m 12s"). "Schedule ▾" span → `<button type="button" disabled className="btn btn-secondary btn-undone" style={pill('secondary')} title="Coming in a later update">Schedule ▾</button>` (§9.1). "Run all now" span → `<button type="button" className="btn btn-primary" style={pill('primary')} disabled={mutation.isPending} onClick={() => mutation.mutate()}>` rendering "Running…" while pending (existing label logic kept). The mutation wiring itself is UNTOUCHED.
2. **Step cards:** grid `repeat(auto-fit, minmax(200px, 1fr))` (6-across ≥1024, 3-across tablet, 2-across phone per §12 row 11 — auto-fit rung 2). StepCard tokenizes: active card `var(--color-accent-tint)` bg with accentText/accent name, else `var(--color-surface-tint)`; status dots ok `var(--color-success)` / warn `var(--color-notify)` / active `var(--color-accent)` (§7.2); name Heading-ish Body 700; times Caption mono.
3. **States (§14.3):** loading — replace "Loading pipeline…" with 6 skeleton step-cards (`.skeleton` blocks ~110px in the same grid) + `aria-busy` + sr-only "Replaying last night's run…". Error — replace bare "Couldn't load pipeline data." with `role="alert"` Body danger-text "Couldn't load the pipeline." + `<button className="btn btn-primary" style={pill('primary')} onClick={() => refetch()}>Retry</button>` (add `refetch` to the useQuery destructure). Empty — when `data && data.steps?.length === 0`: §9.7 idiom "No runs yet" + "The first 6am run writes itself here. You can also trigger one." AND the Run-all Primary button rendered beside it (per §14.3 the pill stays present).
4. **"Last night" card:** header keeps date + clean-run pill via success-tint/success-text tokens; card via `card()`.
  </action>
  <verify>
    <automated>cd /Volumes/1TBFULLYPAID/Projects/yahshua/learnit && npm run build && grep -q "Replaying last night's run" app/\(dashboard\)/pipeline/page.js && grep -q "Couldn't load the pipeline." app/\(dashboard\)/pipeline/page.js && grep -q "No runs yet" app/\(dashboard\)/pipeline/page.js && grep -q "btn-undone" app/\(dashboard\)/pipeline/page.js && grep -q "mutation.mutate" app/\(dashboard\)/pipeline/page.js && grep -q "pipeline/run" app/\(dashboard\)/pipeline/page.js && ! grep -q "Loading pipeline" app/\(dashboard\)/pipeline/page.js && echo PIPELINE-STATES-OK</automated>
  </verify>
  <done>Build passes; §14.3 states land; Run-all remains a working Primary button driving the same mutation; Schedule is an honest disabled button.</done>
  <acceptance_criteria>
  - Live check (port 3197, playwright-core or manual): clicking "Run all now" updates lastRunAt/step times via the existing onSuccess setQueryData (D-11 behavior parity)
  - `grep -c "btn-undone" "app/(dashboard)/pipeline/page.js"` ≥ 1 (Schedule)
  - /tmp/shots/360-pipeline.png shows 2-across step cards
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Ladder reflow + banners + run log responsive with tokens</name>
  <files>app/(dashboard)/pipeline/page.js</files>
  <read_first>
  - .planning/phases/02-ui-design-responsive-overhaul/02-UI-SPEC.md — §12 row 11 (ladder: desktop row with → arrows; tablet 2×2 with ↓; phone vertical stack with ↓; banners full-width; run log full-width mono below), §7.3 (status ladder colors for the four rung tiles), §9.2 (dark card = run log), §6 (mono Caption), §10 (on-dark lime focus ring)
  - app/(dashboard)/pipeline/page.js (after Task 1 — ladder/banners/log region)
  - .planning/phases/02-ui-design-responsive-overhaul/02-RESEARCH.md — Pattern 3 rung 4 (axis swap is structural), anti-pattern "fixed pixel widths on flex children" (the 400px log)
  </read_first>
  <action>
1. **Ladder:** keep the four rung tiles (new 7 / fetched 12 / sorted 9 / done 380) and their data; container becomes viewport-aware via `useViewport()`: desktop — current flex row with `→` separators; tablet — `display:'grid', gridTemplateColumns:'repeat(2, 1fr)'` with a `↓` Caption arrow under each of the first two rungs; phone — flex column, each rung full-width with a `↓` separator between (§12 row 11). Rung tiles: new rung `var(--color-accent)` bg white text (§7.3); fetched/sorted `var(--color-surface-tint)` with ink numerals + sorted getting accentText label color; done `var(--color-success-tint)` with successText label; numerals Display 32 `lineHeight:1` (absorbs the current 20px — §6 exception); labels Caption. Arrows `var(--color-text-faint)` (decorative, aria-hidden).
2. **Banners:** "13 rows sat at failed" banner tokenizes (danger-text on `var(--color-danger-tint)` per §7.2); "Retry all" span → `<button type="button" disabled className="btn btn-danger btn-undone" style={pill('danger')} title="Coming in a later update">Retry all</button>` (§9.1 danger-soft variant; Phase 5 wires it). "New subject created" banner: Rename/Keep spans → disabled `.btn .btn-secondary .btn-undone` buttons with the same title. Both banners full-width on phone (they already stack in the column — ensure no fixed widths).
3. **Run log:** desktop keeps `maxWidth:400` beside the ladder; tablet/phone (useViewport !== 'desktop') render it BELOW full-width (flex column). Log card via `card({dark:true})`; entries `fontFamily: tokens.type.mono`, Caption 12, `lineHeight:2`; timestamps on-dark-faint; highlight spans keep lime/danger-bright semantics tokenized; "Copy log"/"Open skill" spans → `<button type="button" disabled className="btn btn-ghost btn-undone on-dark" style={pill('ghost')} title="Coming in a later update">` (on-dark swaps the focus ring to lime). RunLogLine splitting logic unchanged.
  </action>
  <verify>
    <automated>cd /Volumes/1TBFULLYPAID/Projects/yahshua/learnit && npm run build && grep -q "useViewport" app/\(dashboard\)/pipeline/page.js && grep -q "JetBrains\|type.mono\|mono" app/\(dashboard\)/pipeline/page.js && grep -q "btn-danger" app/\(dashboard\)/pipeline/page.js && grep -q "on-dark" app/\(dashboard\)/pipeline/page.js && echo PIPELINE-LADDER-OK</automated>
  </verify>
  <done>Build passes; ladder renders row/2×2/column per width, log reflows with mono tokens, banners and log buttons follow the honest-disabled idiom.</done>
  <acceptance_criteria>
  - Viewport sweep: `node scripts/check-viewports.mjs --port 3197 --routes /pipeline` — zero overflow at 360–1440
  - /tmp/shots/360-pipeline.png shows the vertical ↓ ladder and full-width mono run log; /tmp/shots/1024-pipeline.png shows the desktop → ladder + 400px side log (D-05 proportions kept)
  - `grep -q "Retry all" "app/(dashboard)/pipeline/page.js"` and its button carries `disabled`
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → GET /api/pipeline | read-only fetch (established) |
| browser → POST /api/pipeline/run | the ONE live mutation — pre-existing, unauthenticated, in-memory only; this plan must not alter it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07-01 | Tampering | POST /api/pipeline/run reachable by direct curl | accept | Pre-existing Phase 1 surface; mutates only the in-memory mock store (resets on restart); no persistence, no PII; unchanged by this plan |
| T-02-07-02 | Tampering (XSS) | run log entry rendering | accept | RunLogLine composes plain JSX spans from store strings; keep the existing split logic (no innerHTML) |
| T-02-07-03 | Elevation | disabled Retry-all/Rename/Keep/Copy-log buttons | accept | Disabled with no handlers; Phase 5/4 wire them |
| T-02-07-04 | DoS | rapid Run-all clicking | accept | `disabled={mutation.isPending}` guards re-entry (existing behavior preserved) |
</threat_model>

<verification>
- `npm run build` passes.
- Viewport sweep (port 3197): `/pipeline` clean at all five widths.
- Behavior parity (D-11): Run-all round-trip updates the UI (same onSuccess path); step/log data untouched (Phase 5 fence).
- Tone: §14.3 pipeline states verbatim; no bare loading/error strings remain.
</verification>

<success_criteria>
- The pipeline story (steps → ladder → log) reads top-to-bottom on a phone and side-by-side on desktop.
- The only working action stays working; every other action is honestly labeled as pending.
- Zero horizontal scroll at 360px on the app's widest-content screen.
</success_criteria>

<output>
After completion, create `.planning/phases/02-ui-design-responsive-overhaul/02-07-SUMMARY.md`
</output>
