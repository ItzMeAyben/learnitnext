---
phase: 02-ui-design-responsive-overhaul
plan: 06
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified:
  - app/(dashboard)/library/page.js
autonomous: true
requirements: [UI-03, UI-04, UI-05]
must_haves:
  truths:
    - "The Library is fully usable at 360px: stats cards in a 2-column grid (failed card spanning 2), filter pills wrapping at 44px, and each table row rendered as a §9.5 stacked card (title + status pill, meta line, failed error line) — no horizontal scroll"
    - "Sorting and filtering still work identically — the @tanstack/react-table instance is kept and only the row PRESENTATION swaps"
    - "The §8 library empty state renders verbatim when the table has no rows: 'Nothing saved yet' + the send-your-first-link body + ghost pill 'See the four doors' → /onboarding"
    - "Loading shows 6 row skeletons with sr-only 'Fetching your saves…' and errors show 'Couldn't load the library — check the pipeline.' + Retry with role=alert — no bare 'Loading…' text anywhere"
    - "'Search transcripts', 'Export CSV', and '+ Save a link' are honest disabled .btn-undone buttons (title 'Coming in a later update') — Phase 4 actions, not dead spans"
  artifacts:
    - path: "app/(dashboard)/library/page.js"
      provides: "Responsive library: stats grid, 44px filter buttons, table≥768 / stacked cards <768, §8+§14.3 states"
      contains: "Nothing saved yet"
  key_links:
    - from: "app/(dashboard)/library/page.js"
      to: "/api/library"
      via: "existing useQuery + useReactTable over the same data (logic untouched)"
      pattern: "useReactTable"
    - from: "app/(dashboard)/library/page.js"
      to: "lib/tokens.js"
      via: "tokens/statusPill/useViewport for the phone card swap"
      pattern: "tokens\\.js"
---

<objective>
Make the Library responsive and welcoming (D-04/D-05: the table becomes §9.5 stacked cards on phone while @tanstack/react-table keeps running sort/filter underneath; stats reflow 5→3→2; filter pills become 44px buttons) and land its §8/§14.3 tone states — the flagship empty state copy straight from the UI-SPEC copywriting contract.

Purpose: UI-03 (360px), UI-04 (empty/loading/error/filter-empty copy), UI-05 (one idiom for tables→cards reused by subject detail in 02-03). D-11 fence: no navigation regressions — the Library screen's interactive controls are its filters/sort (kept working) and its action pills (honestly disabled, Phase 4).

Output: rewritten `app/(dashboard)/library/page.js`. Data logic (columns, sorting, filtering, counts) is preserved — presentation only.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ui-design-responsive-overhaul/02-CONTEXT.md
@.planning/phases/02-ui-design-responsive-overhaul/02-RESEARCH.md
@app/(dashboard)/library/page.js

<interfaces>
From lib/tokens.js (created in 02-01):
```js
export const tokens            // color/space/radius/type/status (status: AA-refined done #15703c / failed #b03d12)
export function pill(variant)       // layout-only; className="btn btn-<variant>"
export function card(opts)
export function statusPill(status)  // §7.3 pill style object
export function useViewport()       // 'phone' triggers the table→cards DOM swap
```
CSS classes (globals.css from 02-01): `.btn .btn-primary .btn-secondary .btn-ghost .btn-danger .btn-undone`, `.skeleton`, `.only-phone .only-tablet .only-desktop`.
OWNERSHIP RULE: no inline background/color on elements carrying .btn-* classes (inactive filter pills take .btn-ghost for its hover; the ACTIVE filter pill drops the variant class and sets ink colors inline — it is not expected to hover).
Table semantics (§9.5/§11.3): ≥768 renders grid rows with role="table"/"row"/"cell", Caption-12 uppercase headers with aria-sort on sortables, and an sr-only caption "Your saved links"; phone renders role="list"/"listitem" cards with headers dropped (consistent role model per width).
Dev-server protocol: scratch port 3196; `node scripts/check-viewports.mjs --port 3196 --routes /library`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Header, stats grid, filter buttons, action-pill idiom, §8/§14.3 tone states</name>
  <files>app/(dashboard)/library/page.js</files>
  <read_first>
  - .planning/phases/02-ui-design-responsive-overhaul/02-UI-SPEC.md — §8 (copywriting contract: library empty state VERBATIM), §12 row 10 (stats 5-across → 3-across tablet → 2-col phone with 5th spanning 2; filters wrap 2 rows at 44px), §14.3 (library loading/error/empty-filter copy), §9.1 (not-yet-wired pills: Search/Export/Save disabled full-opacity), §9.6 (skeleton), §6 (Display 32 h1, stat numerals Display 32 lh 1)
  - .planning/phases/02-ui-design-responsive-overhaul/02-CONTEXT.md — D-04, D-09, and the specifics section ("Library already uses @tanstack/react-table; responsive restack should keep the library (data logic) and change only presentation")
  - app/(dashboard)/library/page.js (the file being rewritten — keep useQuery, counts useMemo, filteredData useMemo, columns useMemo, useReactTable wiring, formatAdded, GRID_COLUMNS constant)
  - .planning/phases/02-ui-design-responsive-overhaul/02-RESEARCH.md — "Don't Hand-Roll" row "Responsive table" (keep the instance, swap the renderer), Pattern 3 rung 4
  </read_first>
  <action>
Rewrite the chrome and states (the table itself is Task 2):

1. **Header:** h1 "Library" Display 32 (already close — normalize tokens); sub-line keeps the existing friendly copy "…you never type in here, the agent fills it in" (a §14.2 exemplar — keep verbatim); count prefix `{total} saves ·`. Action pills → honest disabled buttons: "⌕ Search transcripts" and "Export CSV" as `<button type="button" disabled className="btn btn-secondary btn-undone" style={pill('secondary')} title="Coming in a later update">`, "+ Save a link" as `<button type="button" disabled className="btn btn-primary btn-undone" style={pill('primary')} title="Coming in a later update">` (§9.1 — Phase 4 actions render enabled-looking but do nothing until wired; NO onClick). On phone the header stacks (title block above full-width-stacking pills via flexWrap).
2. **Stats:** `useViewport()` picks the grid — phone: `repeat(2, 1fr)` with the failed (ink) card `gridColumn:'span 2'`; tablet+desktop: `repeat(auto-fit, minmax(180px, 1fr))` (5-across ≥1024, 3-across tablet per §12 row 10). Numerals Display 32 `lineHeight:1`; failed numeral `var(--color-danger-bright)` on ink card; labels Caption `var(--color-text-secondary)` / on-dark-secondary for the ink card.
3. **Filters:** each filter becomes `<button type="button" className={isActive ? 'btn' : 'btn btn-ghost'} style={{...pill('ghost'), minHeight:44, padding:'8px 16px', fontSize:12.5, fontWeight:600, ...(isActive ? { background:'var(--color-ink)', color:'#fff' } : {})}}>` — the inactive pills get the class hover (surface-sunken), the active pill owns its colors inline (no variant class → no hover conflict). Filter row `flexWrap:'wrap'` (wraps to 2 rows at 360 naturally, each 44px). Keep onClick setStatusFilter and the failed-count suffix logic.
4. **Loading:** replace "Loading…" with 6 skeleton rows (`.skeleton` bars sized like a row: title-width variance optional — 6 full-width 56px r-lg blocks in a stack) + container `aria-busy="true"` + sr-only "Fetching your saves…" (§14.3).
5. **Error:** `<div role="alert">` Body `var(--color-danger-text)` "Couldn't load the library — check the pipeline." + `<button className="btn btn-primary" style={pill('primary')} onClick={() => { /* refetch via useQuery refetch — add refetch to the useQuery destructure */ }}>Retry</button>` (§14.3; the current useQuery destructure must gain `refetch`).
6. **Empty table (§8 VERBATIM):** when `!isLoading && !isError && rows.length === 0` render the §9.7 idiom in the card: icon tile (GLYPHS.text on surface-tint), Heading 20 "Nothing saved yet", Body secondary "Send your first link — save a video to your Learn playlist or forward it to your Telegram bot. It lands here, and the overnight run does the rest.", and ghost pill `Link href="/onboarding" className="btn btn-ghost" style={pill('ghost')}` "See the four doors".
7. **Empty filter (informational, not error):** replace "No rows match this filter." with §14.3's "Nothing at this stage right now." + a Body-secondary line "Switch filters or check back after tonight's run." styled per §9.7 minus the CTA.
  </action>
  <verify>
    <automated>cd /Volumes/1TBFULLYPAID/Projects/yahshua/learnit && npm run build && grep -q "Nothing saved yet" app/\(dashboard\)/library/page.js && grep -q "See the four doors" app/\(dashboard\)/library/page.js && grep -q "Fetching your saves" app/\(dashboard\)/library/page.js && grep -q "Couldn't load the library" app/\(dashboard\)/library/page.js && grep -q "Nothing at this stage right now" app/\(dashboard\)/library/page.js && grep -q "btn-undone" app/\(dashboard\)/library/page.js && ! grep -q "Loading…" app/\(dashboard\)/library/page.js && echo LIBRARY-STATES-OK</automated>
  </verify>
  <done>Build passes; §8 empty copy lands verbatim with its /onboarding ghost pill; loading/error/filter-empty use §14.3 copy; header actions are honest disabled buttons; stats and filters reflow.</done>
  <acceptance_criteria>
  - `grep -q 'role="alert"' "app/(dashboard)/library/page.js"`; `grep -c "btn-undone" "app/(dashboard)/library/page.js"` ≥ 3
  - `grep -q "Send your first link" "app/(dashboard)/library/page.js"` (§8 body verbatim)
  - Sort/filter smoke on dev (port 3196, playwright-core or manual): clicking "new" filter narrows rows; clicking the Title header toggles ▲/▼ and reorders — same behavior as Phase 1 (logic untouched)
  - /tmp/shots/360-library.png shows 2-col stats (failed spanning), wrapped filter row, stacked row cards
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Table→§9.5 stacked-card swap on phone, table semantics on ≥768</name>
  <files>app/(dashboard)/library/page.js</files>
  <read_first>
  - .planning/phases/02-ui-design-responsive-overhaul/02-UI-SPEC.md — §9.5 (table row → mobile card idiom: line 1 title Body 14/700 truncated + status pill right; line 2 caption meta 'author · source · added'; failed rows add error line in danger-text Caption; headers hidden on phone; role model), §11.3 (Library grid keeps table semantics: role="table" + aria-sort on headers + sr-only caption "Your saved links"; status pills on failed rows pair with sr-only "status: failed"), §7.3 (statusPill colors)
  - app/(dashboard)/library/page.js (after Task 1 — the table region being restructured)
  - .planning/phases/02-ui-design-responsive-overhaul/02-RESEARCH.md — Pattern 3 rung 4 (the sanctioned JS swap), "Don't Hand-Roll" responsive-table row, Pitfall 5 (hydration — useViewport's getServerSnapshot returns desktop; one desktop frame on phones is accepted)
  </read_first>
  <action>
Restructure the table region with a two-branch renderer driven by `useViewport()`:

1. **Shared:** keep `table` (useReactTable), `filteredData`, `columns`, `sorting` exactly as constructed. Extract `row.original` access for the card branch — the card branch does NOT use flexRender cells; it reads `row.original` fields directly (title, author, topic, source, added, status, error) so the presentation is a true mobile card, not a squeezed grid.
2. **Desktop/tablet branch (viewport !== 'phone'):** current GRID_COLUMNS grid rows inside a container with `role="table"` + sr-only caption "Your saved links"; header spans get `role="columnheader"` and `aria-sort={column.getIsSorted() === 'asc' ? 'ascending' : column.getIsSorted() === 'desc' ? 'descending' : undefined}` on sortables (§11.3 — currently missing); rows `role="row"`, cells `role="cell"`; header labels Caption 12 uppercase (+0.6px when uppercase); row dividers `1px solid var(--color-divider)`; padding 12px 16px. Status cell uses `statusPill(status)`; failed rows append sr-only "status: failed".
3. **Phone branch (viewport === 'phone'):** render `table.getRowModel().rows` as `role="list"` of cards — each `role="listitem"`, `background:'var(--color-surface)'`, r-lg (`var(--radius-lg)`), padding 12, gap `var(--space-xs)`, stacked below the previous (flex column): line 1 = title (Body 14/700, `overflow:'hidden', textOverflow:'ellipsis'`, minWidth 0) + `statusPill(status)` right-aligned; line 2 = Caption meta `{author} · {source} · {formatAdded(added)}` in `var(--color-text-secondary)`; failed rows add line 3 = error text Caption `var(--color-danger-text)` (the §9.5 failed-error-line rule — currently the error only shows in the desktop title cell; keep it there too). No column headers rendered.
4. Both branches keep `flexRender` usage in the desktop branch only. `minWidth:0` everywhere text can be long.
5. Sorting headers in the desktop branch become `<button type="button">` (SortableHeader's span → button, keeping the toggle handler and ▲/▼ indicator) so the global focus ring covers them.
  </action>
  <verify>
    <automated>cd /Volumes/1TBFULLYPAID/Projects/yahshua/learnit && npm run build && grep -q "useViewport" app/\(dashboard\)/library/page.js && grep -q "aria-sort" app/\(dashboard\)/library/page.js && grep -q "Your saved links" app/\(dashboard\)/library/page.js && grep -q "role=\"list\"" app/\(dashboard\)/library/page.js && grep -q "statusPill" app/\(dashboard\)/library/page.js && grep -q "useReactTable" app/\(dashboard\)/library/page.js && echo LIBRARY-TABLE-OK</automated>
  </verify>
  <done>Build passes; ≥768 renders the semantic grid table with aria-sort; phone renders §9.5 cards including the failed error line; the react-table instance still owns sort/filter.</done>
  <acceptance_criteria>
  - Viewport sweep: `node scripts/check-viewports.mjs --port 3196 --routes /library` — zero overflow at 360/390/768/1024/1440 (the 6-column grid no longer blows out 360px)
  - /tmp/shots/360-library.png shows stacked cards with the failed row's error line visible ("error: 402 paywall…")
  - /tmp/shots/1024-library.png shows the 6-column grid with headers — desktop layout preserved (D-05)
  - Phone screenshot shows NO column-header row; desktop screenshot DOES
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → GET /api/library | read-only fetch of the in-memory store; no user input is sent |
| client state (filter/sort) → render | statusFilter and sorting are client-only view state over fetched rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06-01 | Tampering (XSS) | row.title / row.error rendering | accept | Row data (including the failure error string) renders as JSX text; truncation via CSS ellipsis; no innerHTML |
| T-02-06-02 | Elevation | disabled Search/Export/Save buttons | accept | `disabled` + no handlers — cannot be triggered even by direct DOM events until Phase 4 wires them |
| T-02-06-03 | Denial of Service | 6-col grid render on phone (pre-swap hydration frame) | accept | One desktop frame renders before hydration corrects to cards (documented SSR trade-off, research Pitfall 5); no crash, self-corrects |
| T-02-06-04 | Repudiation | — | accept | No mutation surface; Run/Retry-all intentionally absent from this screen's actions (Phase 5) |
</threat_model>

<verification>
- `npm run build` passes.
- Viewport sweep (port 3196): `/library` clean at all five widths (UI-03 for the heaviest table screen).
- Behavior parity (D-11): sort headers toggle, filters narrow, failed count pill correct, no navigation-labeled dead spans introduced (actions are disabled buttons with title tooltips).
- Tone: bare "Loading…" gone; §8 empty state and §14.3 states render verbatim.
</verification>

<success_criteria>
- Phone library reads as cards with status pills and error lines — not a shrunken spreadsheet.
- The react-table logic is byte-for-byte in spirit: same columns, same sort/filter behavior.
- The empty state is the friendly flagship: tells the user exactly what to do next and links to the four doors.
</success_criteria>

<output>
After completion, create `.planning/phases/02-ui-design-responsive-overhaul/02-06-SUMMARY.md`
</output>
