---
phase: 02-ui-design-responsive-overhaul
plan: 12
type: execute
wave: 5
depends_on: [02-01, 02-02, 02-03, 02-04, 02-05, 02-06, 02-07, 02-08, 02-09, 02-10, 02-11]
files_modified:
  - scripts/check-viewports.mjs
autonomous: false
requirements: [UI-03, UI-04, UI-05]
must_haves:
  truths:
    - "Every FLOW-01..06 truth from Phase 1 passes against the redesigned app (with the single D-01-sanctioned FLOW-01 amendment: fresh visitors see the landing whose CTA enters /onboarding — no deep-route redirect)"
    - "All 11 routes x 5 widths pass the no-horizontal-overflow assertion in BOTH cookie states (onboarded via cookie injection; un-onboarded via fresh profile)"
    - "Static hygiene fences hold: no OnboardingGate remnants, no unencoded dynamic hrefs, no 'outline: none', no dangerouslySetInnerHTML, one h1 per screen, zero onClick navigation spans"
    - "A human has walked the app at phone and desktop widths and approved the welcoming tone, the flash-free cold start, and the bottom tab bar feel (UAT gate)"
  artifacts:
    - path: "scripts/check-viewports.mjs"
      provides: "Final full-route-list sweep tool (route list extended to cover every screen incl. query-string routes, if not already)"
      contains: "learnit_onboarded"
    - path: ".planning/phases/02-ui-design-responsive-overhaul/02-VERIFICATION-EVIDENCE.md"
      provides: "The D-11 fence matrix results + sweep output the verifier/auditor reuses (created by the executor as the record of Task 1)"
      contains: "FLOW-"
  key_links:
    - from: "scripts/check-viewports.mjs"
      to: "every route in the app"
      via: "the full route list: /, /onboarding, /subjects, /subjects/AI%20Agents, /pathway/AI%20Agents, /pathway/AI%20Agents/print, /quiz?subject=AI%20Agents, /listen?subject=AI%20Agents, /library, /pipeline, /session, /nope"
      pattern: "/session"
    - from: "Task 1 evidence"
      to: ".planning/phases/01-connect-screens/01-VERIFICATION.md"
      via: "each FLOW truth re-checked and recorded PASS with its new evidence line"
      pattern: "FLOW-0"
---

<objective>
Close Phase 2 with the D-11 regression fence and the UI-03 final sweep: re-run the complete FLOW-01..06 matrix from Phase 1 verification against the redesigned app (with the one D-01-sanctioned amendment), sweep every route at every acceptance width in both cookie states, enforce the static hygiene fences, and hand the result to the human for the phase's UAT sign-off (welcoming tone, cold start, bottom bar, notch/safe-area on a real device if available).

Purpose: D-11 is a locked decision ("this is a regression fence") and ROADMAP criterion 3/5 require proof. This plan produces that proof in one place so $gsd-verify-phase and the auditor build on it.

Output: updated `scripts/check-viewports.mjs` (full default route list), a `02-VERIFICATION-EVIDENCE.md` record in the phase dir, and a completed human UAT checkpoint. No app-code changes (any failure found routes back to the owning plan's executor as a fix task under this plan).
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ui-design-responsive-overhaul/02-CONTEXT.md
@.planning/phases/02-ui-design-responsive-overhaul/02-UI-SPEC.md
@.planning/phases/02-ui-design-responsive-overhaul/02-RESEARCH.md
@.planning/phases/01-connect-screens/01-VERIFICATION.md
@scripts/check-viewports.mjs

<interfaces>
The FLOW-01..06 matrix (from 01-VERIFICATION.md "Observable Truths", amended by UI-SPEC §15):

| FLOW | Amended truth to re-prove |
|------|---------------------------|
| FLOW-01 | Fresh profile at `/` sees the LANDING whose primary CTA "Start setup" enters `/onboarding` (deep-route redirect intentionally gone per D-01); Finish/Skip set learnit_onboarded cookie and land on `/` rendering Today; revisit `/` stays Today |
| FLOW-02 | Wizard walks 1→5 via Continue, Back clamps at 1, Set-up-later advances, Finish completes, Skip works from any step (02-10 Task 2 already proved this — re-confirm) |
| FLOW-03 | Top tabs (≥768) and bottom bar (<768) both reach Today, Subjects (index lists all 3), Listen, Library, Pipeline |
| FLOW-04 | From a subject: pathway opens + "Continue where you left off" scrolls to the active stage; quiz starts with ?subject=; listening starts with ?subject=; quiz Exit and Listen "Read instead" return to that subject |
| FLOW-05 | Zero navigation-labeled dead spans — every nav-labeled control is a Link; remaining actions are honestly disabled buttons (span audit: only content spans and sr-only spans remain) |
| FLOW-06 | Unknown subject/pathway/print ids render 404 (client + server); every dynamic href uses encodeURIComponent |

Full route list for the sweep: `/`, `/onboarding`, `/subjects`, `/subjects/AI%20Agents`, `/pathway/AI%20Agents`, `/pathway/AI%20Agents/print`, `/quiz?subject=AI%20Agents`, `/listen?subject=AI%20Agents`, `/library`, `/pipeline`, `/session`, `/nope`.
Dev-server protocol: scratch port 3199 (final sweep owns it exclusively — no parallel plans remain).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-11 FLOW matrix re-run + full-route viewport sweep + hygiene fences + evidence record</name>
  <files>scripts/check-viewports.mjs, .planning/phases/02-ui-design-responsive-overhaul/02-VERIFICATION-EVIDENCE.md</files>
  <read_first>
  - .planning/phases/01-connect-screens/01-VERIFICATION.md — the original FLOW-01..06 truth table, behavioral spot-check commands, and evidence style (the template for the new record)
  - .planning/phases/02-ui-design-responsive-overhaul/02-UI-SPEC.md — §15 (the fence + the FLOW-01 amendment), §3 (acceptance widths), §10 (hygiene: outline:none banned), §11 (semantics)
  - .planning/phases/02-ui-design-responsive-overhaul/02-RESEARCH.md — Pattern 5 (the sweep protocol), Pitfall 8 (cookie-state fixture discipline)
  - scripts/check-viewports.mjs (the 02-01 tool — extend ONLY its default route list if the full route list isn't already the default)
  </read_first>
  <action>
Run everything; write the evidence file; fix-forward small breaks (a broken wire found here is fixed under this task in the OWNING screen file, cross-referenced in the SUMMARY — the fix follows that screen's plan spec):

1. **Prepare:** `npm install --no-save playwright-core`; start `npm run dev -- --port 3199`; create /tmp/shots. The dev server on 3199 STAYS RUNNING after this task — Task 2's human checkpoint hands its URL over (kill it only after the phase closes).
2. **FLOW matrix (playwright-core, two contexts: fresh profile AND cookie-injected):** for each FLOW-01..06 truth in the `<interfaces>` table, run the concrete check (clicks for 01/02/03/04; SSR + DOM audits for 05/06; the 01-VERIFICATION behavioral spot-check table is the command playbook). Record PASS/FAIL + one evidence line each into `.planning/phases/02-ui-design-responsive-overhaul/02-VERIFICATION-EVIDENCE.md` (format: a table mirroring 01-VERIFICATION's "Observable Truths").
3. **Viewport sweep:** `node scripts/check-viewports.mjs --port 3199` (cookie state) and `node scripts/check-viewports.mjs --port 3199 --no-cookie` (un-onboarded state) over the full route list — assert `ALL VIEWPORTS CLEAN` twice. Extend the script's DEFAULT route list to the full list (its `--routes` flag already accepts overrides; make the default complete for future phases).
4. **Hygiene fences (grep):**
   - `grep -rn "OnboardingGate\|router.replace('/onboarding')" app components` → empty
   - `grep -rn "dangerouslySetInnerHTML" app components lib` → empty
   - `grep -rn "outline: none\|outline:none" app` → empty
   - `grep -rn "href={'/subjects/' + \|href={\`/subjects/\${" app components | grep -v encodeURIComponent` → empty (no unencoded dynamic hrefs; same for pathway/quiz/listen prefixes)
   - `grep -rn "'/subjects/AI Agents'\|\"/subjects/AI Agents\"" app components` → empty (no hardcoded unencoded ids)
   - One h1 per screen: for each route in the list, `curl -s http://localhost:3199<route> | grep -c "<h1"` == 1 (client-fetch screens render their h1 post-hydration — for those, count via playwright `document.querySelectorAll('h1').length` instead)
   - Zero onClick navigation spans: `grep -rn "<span[^>]*onClick" app components` → empty (disabled buttons and content spans only)
5. **Build + cold start:** `npm run build` exits 0; fresh-profile headless capture of `/` shows the landing hero headline "Save a link today. Wake up to course material tomorrow." with NO intermediate flash frame (capture at 1280 and 360).
6. **Evidence file:** write `02-VERIFICATION-EVIDENCE.md` with: the FLOW table (PASS/FAIL + evidence), sweep verdict lines, hygiene grep results, the two cold-start captures' paths, and any fix-forward notes. This file is the hand-off to $gsd-verify-phase.
  </action>
  <verify>
    <automated>cd /Volumes/1TBFULLYPAID/Projects/yahshua/learnit && pkill -f "next dev.*3199" 2>/dev/null; npm run build && (npm run dev -- --port 3199 >/tmp/dev-0212.log 2>&1 &) && sleep 6 && node scripts/check-viewports.mjs --port 3199; SW=$?; node scripts/check-viewports.mjs --port 3199 --no-cookie --routes /,/onboarding >/dev/null 2>&1; ! grep -rn "OnboardingGate" app components; ! grep -rn "dangerouslySetInnerHTML" app components; ! grep -rn "outline: none" app; test -f .planning/phases/02-ui-design-responsive-overhaul/02-VERIFICATION-EVIDENCE.md && grep -q "FLOW-06" .planning/phases/02-ui-design-responsive-overhaul/02-VERIFICATION-EVIDENCE.md; curl -s -o /dev/null http://localhost:3199/ && echo SERVER-LEFT-RUNNING-FOR-CHECKPOINT; exit $SW</automated>
  </verify>
  <done>Build passes; both sweeps report ALL VIEWPORTS CLEAN over the full route list; all six FLOW rows PASS with evidence; all hygiene greps empty; evidence file written. The dev server on port 3199 is left RUNNING (verified by the trailing curl) so Task 2's checkpoint can hand over http://localhost:3199/.</done>
  <acceptance_criteria>
  - `node scripts/check-viewports.mjs --port 3199` prints `ALL VIEWPORTS CLEAN` (12 routes × 5 widths, cookie state)
  - Same verdict with `--no-cookie` for `/` and `/onboarding` (landing renders, not a redirect)
  - 02-VERIFICATION-EVIDENCE.md contains six FLOW rows all PASS, each with a concrete evidence line (URL + observed selector/click)
  - Any FAIL: fixed in the owning file per its plan spec, re-run, and the fix noted in the evidence file before this task closes
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human UAT — the welcoming walkthrough (phase gate)</name>
  <read_first>
  - .planning/phases/02-ui-design-responsive-overhaul/02-VERIFICATION-EVIDENCE.md (Task 1's record — the automated half of this gate)
  - .planning/phases/02-ui-design-responsive-overhaul/02-UI-SPEC.md — §13 (landing sections you will walk), §14.1 (Today hierarchy), §12 (the per-screen layouts you are eyeballing)
  - .planning/phases/02-ui-design-responsive-overhaul/02-CONTEXT.md — D-09 (welcoming tone brief), D-11 (what must NOT have changed)
  </read_first>
  <what-built>
The complete Phase 2 redesign: adaptive landing at `/` (no cold-start flash), redesigned Today hub, responsive every-screen (360px→desktop), phone bottom tab bar, token-driven visual system, guided onboarding, friendly empty/loading/error copy, system-joined 404 and print views — with all Phase 1 flows re-verified automatically (Task 1 evidence file).

The app is already running for you at http://localhost:3199/ — the agent started this dev server in Task 1 and left it alive for this checkpoint. You only open the URL in a browser; no commands needed.
  </what-built>
  <how-to-verify>
Open http://localhost:3199/ in a private/empty browser window (private = fresh profile = un-onboarded visitor; the server is already running — nothing to start).

1. **Cold start (UI-01/UI-04):** in that private window, load `http://localhost:3199/` — you should see the LANDING (hero "Save a link today. Wake up to course material tomorrow.", real subject tiles), never a flash of the dashboard, never a bounce to the wizard. Click "Start setup", walk the 5 steps, click "Finish setup" — you land on the redesigned Today with the lime greeting hero.
2. **Today hierarchy (UI-02):** confirm ONE obvious next action ("Continue reading" — click it: it should take you into the AI Agents pathway scrolled at the active stage), subjects grid, and the quiet right rail (now playing / saved yesterday / last night's run). The name should read Iven here AND on /session.
3. **Phone pass (UI-03/D-05):** open DevTools device mode at 360×800 and walk: Today → bottom tab bar (5 destinations, active indicator) → Subjects → a subject → pathway (Move up/Move down buttons) → quiz (stacked footer) → Listen (stacked panes, dark toggle) → Library (stacked cards, failed row shows its error line) → Pipeline (vertical ladder). No horizontal scroll anywhere; everything tappable is at least thumb-sized.
4. **Tone (D-09):** check the empty-filter message on Library ("Nothing at this stage right now.") and the copy on the landing's four-doors section — friendly, plain, no dead ends.
5. **Real device (optional but valuable):** open the app on an iPhone in Safari and confirm the bottom bar clears the home indicator (safe-area) — this cannot be verified headlessly.
6. **Regression sniff (D-11):** visit `/subjects/DoesNotExist` and `/pathway/DoesNotExist` (should show "Nothing here"), and click "Exit" from a quiz started with `?subject=Distribution` (should return to the Distribution subject).
7. Read `.planning/phases/02-ui-design-responsive-overhaul/02-VERIFICATION-EVIDENCE.md` — confirm the six FLOW rows say PASS.
  </how-to-verify>
  <acceptance_criteria>
  - Human returns "approved" (recorded in the session log) — OR lists defects, each routed to its owning plan, fixed, and re-verified before the phase closes
  - Specifically acknowledged in the approval: cold start has no flash; Today has one obvious next action; the 360px walk had no horizontal scroll; the app still greets rather than annoys
  </acceptance_criteria>
  <resume-signal>Type "approved" to close Phase 2, or describe anything that feels wrong (screen, width, what you saw) and the fix will be routed to the owning plan.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verification tooling → dev server | the sweep drives localhost only; playwright-core installed --no-save (package.json untouched) |
| evidence file → downstream verifiers | the record is prose + command output; treat as claims to re-check, not as authority |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-12-01 | Tampering | evidence file could overstate results | mitigate | Every row must carry a reproducible command/selector; the phase verifier re-runs the sweep independently — dishonest rows get caught by the re-run |
| T-02-12-02 | Tampering | fix-forward edits under this plan | mitigate | Fixes must follow the OWNING plan's spec (cited in the evidence file); no opportunistic redesign in a fence plan |
| T-02-12-03 | DoS | sweep opens 12×5×2 contexts | accept | Sequential contexts against a local dev server; runtime minutes; closed as it goes |
| T-02-12-04 | Repudiation | human UAT approval | accept | The checkpoint's resume signal is the record (GSD session log); no cryptographic signature needed for a personal tool |
</threat_model>

<verification>
- `npm run build` exits 0.
- Both viewport sweeps print ALL VIEWPORTS CLEAN over the full route list.
- 02-VERIFICATION-EVIDENCE.md exists with six PASS rows and hygiene results.
- Human checkpoint returned "approved" (or its fixes were applied and re-verified).
</verification>

<success_criteria>
- Phase 1 flows and Phase 2 design coexist — proven, not assumed (D-11).
- The five ROADMAP criteria for Phase 2 each have evidence: landing (sweep + cold-start captures), Today hierarchy (UAT step 2), responsive (sweep), tone (UAT step 4), one visual system (UAT + spot-checks).
- The next phase (3 — Live Data Wiring) starts from a clean, verified baseline.
</success_criteria>

<output>
After completion, create `.planning/phases/02-ui-design-responsive-overhaul/02-12-SUMMARY.md`
</output>
