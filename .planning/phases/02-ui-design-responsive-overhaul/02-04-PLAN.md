---
phase: 02-ui-design-responsive-overhaul
plan: 04
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified:
  - app/(dashboard)/pathway/[subjectId]/page.js
  - app/pathway/[subjectId]/print/page.js
autonomous: true
requirements: [UI-03, UI-04, UI-05]
must_haves:
  truths:
    - "The pathway screen is fully usable at 360px: dot rail shrinks to 28px with 12px dots, stages stack 1-column, and reordering uses two full-width 44px 'Move up'/'Move down' Ghost buttons instead of 20px-tall arrow spans"
    - "The subject chips row scrolls horizontally inside its own snap container on phone (page itself never scrolls sideways)"
    - "'Continue where you left off' still scrolls to the active stage, and respects prefers-reduced-motion (behavior:'auto')"
    - "The print/export page is token-styled (ink borders, type scale, r-lg) and its @media print output hides the back link and avoids page breaks inside stage rows"
    - "Every Phase 1 wire survives: encoded breadcrumbs/print/listen/quiz hrefs, the notFound() guard, and the per-subject reorder state"
  artifacts:
    - path: "app/(dashboard)/pathway/[subjectId]/page.js"
      provides: "Responsive pathway: hero, chips snap row, vertical timeline, phone reorder row, §14.3 states"
      contains: "Move up"
    - path: "app/pathway/[subjectId]/print/page.js"
      provides: "Token-joined print sheet with @media print rules (D-08)"
      contains: "no-print"
  key_links:
    - from: "app/(dashboard)/pathway/[subjectId]/page.js"
      to: "app/pathway/[subjectId]/print/page.js"
      via: "existing encoded '⎙ Print / export' Link (D-11 — unchanged href)"
      pattern: "/print"
    - from: "app/(dashboard)/pathway/[subjectId]/page.js"
      to: "lib/tokens.js"
      via: "tokens + useViewport for rail/rail-below structural swap"
      pattern: "tokens\\.js"
---

<objective>
Make the pathway screen responsive (D-04/D-05: timeline goes compact-vertical on phone, reorder arrows become 44px "Move up"/"Move down" Ghost pills, chips scroll-snap in-container, the 300px right rail moves below stages <1024px) and bring the print/export sheet into the visual system (D-08): token styling, 100% width on phone, and real @media print rules.

Purpose: UI-03 (360px usability), UI-04 (§14.3 pathway loading/empty copy), UI-05 (print view joins the system — roadmap criterion 5). D-11 fence: encoded hrefs, quiz/listen hops, active-stage scroll machinery, and 404s survive verbatim.

Output: rewritten `app/(dashboard)/pathway/[subjectId]/page.js` and `app/pathway/[subjectId]/print/page.js`. Data stays mock (Phase 3 fence).
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ui-design-responsive-overhaul/02-CONTEXT.md
@.planning/phases/02-ui-design-responsive-overhaul/02-RESEARCH.md
@app/(dashboard)/pathway/[subjectId]/page.js
@app/pathway/[subjectId]/print/page.js

<interfaces>
From lib/tokens.js (created in 02-01):
```js
export const tokens          // color/space/radius/type/status maps (§7.2 values 1:1)
export function pill(variant)     // layout-only; pair with className="btn btn-<variant>"
export function card(opts)        // surface card style object
export function statusPill(status)
export function useViewport()     // 'phone' | 'tablet' | 'desktop' — for the rail placement + dot-rail sizing swaps
```
CSS classes (globals.css from 02-01): `.btn .btn-primary .btn-secondary .btn-ghost .btn-undone`, `.crumb-link`, `.skeleton`, `.scroll-snap-x .snap-item` (sanctioned in-container horizontal scroll), `.no-print`, `.print-row` (break-inside:avoid), `.only-phone .only-tablet .only-desktop`.
OWNERSHIP RULE: no inline background/color/opacity/transform/outline on any element that carries a state class (.btn-*, .crumb-link).
The print page is a SERVER component (async, awaits params, calls notFound()) and imports token VALUES from lib/tokens.js directly — that import only works because tokens.js has no 'use client' directive (02-01 Task 2).
Dev-server protocol: scratch port 3194; `node scripts/check-viewports.mjs --port 3194 --routes /pathway/AI%20Agents,/pathway/AI%20Agents/print,/pathway/DoesNotExist`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pathway responsive — compact timeline, phone reorder row, snap chips, §14.3 states</name>
  <files>app/(dashboard)/pathway/[subjectId]/page.js</files>
  <read_first>
  - .planning/phases/02-ui-design-responsive-overhaul/02-UI-SPEC.md — §12 row 6 (pathway layouts: dot rail 64px→28px with 12px dots on phone; stages 1-col; reorder arrows → 'Move up'/'Move down' 44px Ghost row; checklist rows keep; hero stats stack; chips scroll-snap row in-container), §10 (touch targets row — pathway reorder rule; reduced motion row — scrollIntoView must check matchMedia), §9.9 (breadcrumbs), §9.10 (hero), §14.3 (pathway copy)
  - .planning/phases/02-ui-design-responsive-overhaul/02-CONTEXT.md — D-05 (pathway timeline scrolls vertically), D-09, D-11 (fence: breadcrumbs, encoded hrefs, 404, reorder state)
  - app/(dashboard)/pathway/[subjectId]/page.js (the file being rewritten — preserve decorateStage logic, orders/moveStage per-subject state, #active-stage id + scroll, ALL encoded hrefs, `if (!name || (subjects && !subject)) notFound()`)
  - node_modules/next/dist/docs/01-app/03-api-reference/04-functions/not-found.md (guard stays in render path)
  - .planning/phases/02-ui-design-responsive-overhaul/02-RESEARCH.md — Pattern 3 (rung 4 for rail/dot-rail swaps), Pitfall 7 (FLOW regression)
  </read_first>
  <action>
Rewrite per §12 row 6, preserving Phase 1 semantics (D-11):

1. **Breadcrumb:** Links ("Subjects", "{subject.id}") get `className="crumb-link"` (no inline color); current "Pathway" ink 700; 44px min-height crumbs.
2. **Header actions:** "⎙ Print / export" and "♪ Switch to Listen" stay Links → `className="btn btn-secondary" style={pill('secondary')}` (hrefs unchanged, encoded). "Continue where you left off" becomes a real `<button className="btn btn-primary" style={pill('primary')}>` whose onClick scrolls: `document.getElementById('active-stage')?.scrollIntoView({ behavior: matchMedia('(prefers-reduced-motion: reduce)').matches ? 'auto' : 'smooth' })` (§10). On phone the action row wraps full-width.
3. **Subject chips row:** wrap in `className="scroll-snap-x"` with each chip a `.snap-item` (flex:none) — the ONE sanctioned in-container horizontal scroll; chips keep active-ink vs surface styling (class-free active state may stay inline since chips are Links styled per state via JS — acceptable: no hover prescribed).
4. **Hero banner (§9.10):** subject-color bg r-2xl, padding xl desktop / md phone (`useViewport()`); eyebrow chip; h1 Display 32 (down from 36); stats block (`{done} / {n} stages` + 220px progress bar) moves BELOW the copy on phone — stack via `useViewport() === 'phone' ? column : row-end` (rung 4); progress bar `width:'100%', maxWidth:220` on phone. Lime fill on colored hero (§9.11).
5. **Timeline:** `useViewport()` picks rail metrics — desktop/tablet `{rail:64, dot:14, line:3}`, phone `{rail:28, dot:12, line:2}` (§12 row 6). Dots: done `var(--color-success)`, active `var(--color-accent)`, pending `var(--color-border)`; connector lines `var(--color-border-strong)`/success. Stage list `flex:1, minWidth:0` (kills the current minWidth:320 blowout at 360).
6. **Stage cards:** tokenize (icon tiles per §7.2 tints, "IN PROGRESS" pill accent-tint/accent-text, r-xl cards, checklist rows surface-tint r-md). "Review"/"Continue" stay Links/buttons with `.btn .btn-ghost` + `pill('ghost')`; locked "Locked" stays a non-interactive pill.
7. **Reorder (§10):** desktop/tablet keep compact arrows but as `<button aria-label="Move stage up">`/`"Move stage down"` (44×44 hit area, glyph centered, `var(--color-surface-tint)` r-sm, cursor pointer) — no more 24×20 spans. Phone: replace the arrow pair with a full-width row of two Ghost pills — `<button className="btn btn-ghost" style={{...pill('ghost'), width:'100%'}}>Move up</button>` and `Move down` (each 44px, side by side, `flex:1`) — same moveStage(i, ±1) handlers.
8. **Right rail (builds-from + certificate):** desktop renders beside stages (`width:300, flex:'none'`); tablet/phone renders BELOW the stage list (`useViewport() === 'desktop' ? row : column` — rung 4; §12 row 6 "rail moves below stages"). Certificate ink card tokenizes via `card({dark:true})` + lime progress.
9. **States (§14.3):** loading → 4 stage-card skeletons + sr-only "Laying out your reading path…" (replaces bare `Loading…`); error → `role="alert"` + "Couldn't load — check the pipeline." (existing copy kept) + Retry `.btn .btn-primary`; empty → when `subject.stages?.length === 0`: §9.7 idiom "Pathway not built yet" + "This subject needs a couple more sources before its stages lock in." + `Link className="btn btn-link"` "Back to subject →" → `/subjects/${encodeURIComponent(subject.id)}`.
10. `notFound()` guard, per-subject `orders` state, and every `encodeURIComponent` href survive untouched.
  </action>
  <verify>
    <automated>cd /Volumes/1TBFULLYPAID/Projects/yahshua/learnit && npm run build && grep -q "Move up" app/\(dashboard\)/pathway/\[subjectId\]/page.js && grep -q "scroll-snap-x" app/\(dashboard\)/pathway/\[subjectId\]/page.js && grep -q "prefers-reduced-motion" app/\(dashboard\)/pathway/\[subjectId\]/page.js && grep -q "Laying out your reading path" app/\(dashboard\)/pathway/\[subjectId\]/page.js && grep -q "Pathway not built yet" app/\(dashboard\)/pathway/\[subjectId\]/page.js && grep -q "useViewport" app/\(dashboard\)/pathway/\[subjectId\]/page.js && grep -c "encodeURIComponent" app/\(dashboard\)/pathway/\[subjectId\]/page.js && echo PATHWAY-OK</automated>
  </verify>
  <done>Build passes; pathway renders compact timeline + Move up/Move down row at 360, snap-scrolling chips, token styling, §14.3 states, and all Phase 1 wires intact.</done>
  <acceptance_criteria>
  - `grep -c "encodeURIComponent" "app/(dashboard)/pathway/[subjectId]/page.js"` ≥ 6 (breadcrumb, print, listen, chips, quiz hop, empty-state link)
  - `grep -q "aria-label=\"Move stage up\"" "app/(dashboard)/pathway/[subjectId]/page.js"` — desktop arrows are labeled buttons
  - Viewport sweep: `node scripts/check-viewports.mjs --port 3194 --routes /pathway/AI%20Agents,/pathway/DoesNotExist` — no overflow at 360–1440; DoesNotExist shows the 404
  - /tmp/shots/360-pathway_AI_Agents.png shows the narrow dot rail, full-width Move up/Move down Ghost pair on the active stage, and the rail cards below the timeline
  - Dev-server click check (playwright-core or manual): "Continue where you left off" scrolls #active-stage into view; "Move up" reorders stages (per-subject state intact)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Print sheet joins the system (D-08) — tokens + @media print rules</name>
  <files>app/pathway/[subjectId]/print/page.js</files>
  <read_first>
  - .planning/phases/02-ui-design-responsive-overhaul/02-UI-SPEC.md — §12 row 13 (print/export: 960px sheet, tokens applied — ink borders, type scale, r-lg; maxWidth 100% + padding md on phone; @media print hides back link, white bg, avoid page-break inside stage rows), §6 (type roles), §8 (no copy changes needed — keep existing copy), D-08 in 02-CONTEXT.md
  - app/pathway/[subjectId]/print/page.js (the file being modified — keep `await params`, decode try/catch → notFound(), getSubjectById, encoded back link, date formatting)
  - lib/tokens.js (server-safe import of token values — no directive on that file)
  - node_modules/next/dist/docs/01-app/03-api-reference/04-functions/not-found.md (server notFound() usage stays)
  </read_first>
  <action>
Join the print sheet to the system per §12 row 13 (D-08), changing ONLY styling — no behavior:

1. Import `{ tokens }` from `'../../../../lib/tokens.js'` and style from token values: sheet `background:'var(--color-surface)'` (or `#fff` via `tokens.color.surface`), outer page `var(--color-canvas-deep)`, header/footer rules `var(--color-ink)` 2px, stage dividers `var(--color-border)`, meta text `var(--color-text-secondary)` (NOT #8b889f — §7.6 ban), dot colors success/accent/text-faint tokens.
2. Type scale (§6): h1 `{subject.id}` Display 32 (from 30); stage titles Heading 20 (from 14.5 → 20/700/-0.3); stage descs Body 14; "READING PATHWAY" eyebrow Caption 12/700/+0.6 uppercase; "Exported {date}" Caption.
3. Radii: sheet stays square-edged for print fidelity EXCEPT stage dots and the logo mark which use `tokens.radius.sm`/`tokens.radius.md` (r-lg rule applies to screen rendering; print sheet keeps its document look per §12 row 13 "tokens applied (ink borders, type scale, r-lg)" — give the outer sheet `borderRadius: tokens.radius.lg` on screen).
4. Responsive: sheet `width:960, maxWidth:'100%'` (already maxWidth 100% — keep) and outer padding `var(--space-lg)` desktop / `var(--space-md)` phone via `clamp(16px, 4vw, 40px)` (rung 1).
5. @media print via the 02-01 class layer: back Link gets `className="no-print"`; each stage row div gets `className="print-row"`; body print bg comes from the global rule. Do NOT inline `@media` (impossible) and do NOT add a second stylesheet.
6. Keep `await params`, the decode-guard `notFound()`, `getSubjectById` + `if (!subject) notFound()`, and the encoded "← Back to pathway" href exactly as-is (D-11).
  </action>
  <verify>
    <automated>cd /Volumes/1TBFULLYPAID/Projects/yahshua/learnit && npm run build && grep -q "no-print" app/pathway/\[subjectId\]/print/page.js && grep -q "print-row" app/pathway/\[subjectId\]/print/page.js && grep -q "lib/tokens.js" app/pathway/\[subjectId\]/print/page.js && grep -q "notFound()" app/pathway/\[subjectId\]/print/page.js && grep -q "encodeURIComponent" app/pathway/\[subjectId\]/print/page.js && curl-skip() { :; } && echo PRINT-OK</automated>
  </verify>
  <done>Build passes; print sheet styled from tokens with no-print/print-row classes and unchanged data/notFound/back-link behavior.</done>
  <acceptance_criteria>
  - `curl -s http://localhost:3194/pathway/Distribution/print | grep -c "no-print"` ≥ 1 (back link hidden on paper)
  - `curl -s http://localhost:3194/pathway/Nope/print -o /dev/null -w "%{http_code}"` returns 404 (guard intact — D-11)
  - Viewport sweep includes `/pathway/AI%20Agents/print` at 360 — no overflow (sheet maxWidth 100% works)
  - Chrome's print emulation (playwright-core `page.emulateMedia({ media: 'print' })` screenshot at /tmp/02-04-print-emulated.png) shows the back link absent and white background
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → GET /api/subjects (pathway) | read-only; subjectId decoded in try/catch and matched against the list before render |
| server render → lib/store.js (print) | print page reads the in-memory store directly server-side (established Phase 1 pattern, unchanged) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | Tampering (XSS) | pathway stage/store rendering | accept | JSX text only; keep `dangerouslySetInnerHTML` absent |
| T-02-04-02 | Tampering | subjectId → decode → lookup | accept | Existing guards preserved verbatim (decode try/catch, list-match null → notFound(), server getSubjectById → notFound()); no new sinks |
| T-02-04-03 | Denial of Service | scrollIntoView on repeated clicks | accept | Native browser behavior; reduced-motion variant is cheaper (auto); no loop risk |
| T-02-04-04 | Elevation | reorder buttons | accept | Client-only per-subject view state (existing Phase 1 CR-01 pattern); persists nowhere; no server mutation |
</threat_model>

<verification>
- `npm run build` passes.
- Viewport sweep (port 3194): `/pathway/AI%20Agents`, `/pathway/AI%20Agents/print`, `/pathway/DoesNotExist` clean at all five widths.
- D-11 spot re-checks: breadcrumb Links, print Link, "Switch to Listen" hop, stage "Review" → quiz hop all still encoded; unknown pathway renders 404; #active-stage scroll works after the button conversion.
- Reduced motion: with `reducedMotion: 'reduce'` (the check script sets it), the page renders without shimmer/skeleton animation and Continue jumps instantly.
</verification>

<success_criteria>
- Pathway is thumb-usable at 360px: 44px Move up/Move down, compact dots, no horizontal page scroll.
- Print sheet is unmistakably the same product (tokens, type) and prints clean (no back link, no split stage rows).
- Zero FLOW-04/FLOW-06 regressions on this screen.
</success_criteria>

<output>
After completion, create `.planning/phases/02-ui-design-responsive-overhaul/02-04-SUMMARY.md`
</output>
