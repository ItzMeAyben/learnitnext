---
phase: 02-ui-design-responsive-overhaul
plan: 03
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified:
  - app/(dashboard)/subjects/page.js
  - app/(dashboard)/subjects/[subjectId]/page.js
autonomous: true
requirements: [UI-03, UI-04, UI-05]
must_haves:
  truths:
    - "The subjects index is fully usable at 360px: featured tile then 1-column list tiles, no horizontal scroll, no clipped text"
    - "The subjects index shows friendly loading (5 tile skeletons + sr-only 'Sorting last night's haul…') and empty copy ('No subjects yet' + what happens next) instead of bare 'Loading…'"
    - "Subject detail stacks cleanly at 360px: hero with 2×2 stat wrap, 1-column material cards, and the sources list as §9.5 stacked cards — not a 4-column grid"
    - "Every interactive element on both screens is a semantic Link or button ≥44px (Rename / Open in Gemini / Rebuild become disabled .btn-undone buttons, Phase 4 actions stay unwired)"
    - "Both screens style exclusively from tokens (var(--…) / lib/tokens.js) — zero raw hex values outside statusPill data"
  artifacts:
    - path: "app/(dashboard)/subjects/page.js"
      provides: "Responsive subjects index with skeleton/empty/error states"
      contains: "No subjects yet"
    - path: "app/(dashboard)/subjects/[subjectId]/page.js"
      provides: "Responsive subject hub: hero, material cards, sources as table≥768 / stacked cards <768"
      contains: "useViewport"
  key_links:
    - from: "app/(dashboard)/subjects/[subjectId]/page.js"
      to: "lib/tokens.js"
      via: "import { tokens, pill, statusPill, useViewport } (02-01 contract)"
      pattern: "from '../../../lib/tokens\\.js'|from \"../../../lib/tokens\\.js\"|tokens\\.js"
    - from: "app/(dashboard)/subjects/page.js"
      to: "app/(dashboard)/subjects/[subjectId]/page.js"
      via: "existing encodeURIComponent subject Links (D-11 — unchanged hrefs)"
      pattern: "encodeURIComponent"
---

<objective>
Bring the two subjects screens into the design system and make them fully responsive (D-04/D-06): the subjects index gets its §12 row-4 layout (featured colored tile + 2-col list tiles ≥768, 1-col on phone) and its §14.3 tone states; subject detail gets its §12 row-5 layout (hero + 360px provenance card on desktop, all-stacked on phone with 2×2 hero stats), its sources table converted to §9.5 stacked cards on phone, and its action spans converted to the §9.1 disabled-button idiom.

Purpose: UI-03 (360px usability), UI-04 (friendly loading/empty/error copy), UI-05 (one visual system). D-11 fence: every Phase 1 Link (subject tiles, 6 launchers, breadcrumb) keeps its exact encoded href; the 404 guard stays.

Output: rewritten `app/(dashboard)/subjects/page.js` and `app/(dashboard)/subjects/[subjectId]/page.js`. No data changes — screens keep rendering the current store/API data (Phase 3 owns realism).
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ui-design-responsive-overhaul/02-CONTEXT.md
@.planning/phases/02-ui-design-responsive-overhaul/02-RESEARCH.md
@app/(dashboard)/subjects/page.js
@app/(dashboard)/subjects/[subjectId]/page.js

<interfaces>
From lib/tokens.js (created in 02-01):
```js
export const tokens         // tokens.color.*, tokens.space.*, tokens.radius.*, tokens.type.*, tokens.status
export function pill(variant)    // 'primary'|'secondary'|'ghost'|'danger' → layout-only props; pair with className="btn btn-<variant>"
export function card(opts)       // {background:'var(--color-surface)', borderRadius:'var(--radius-xl)', padding:'var(--space-lg)', display:'flex', flexDirection:'column', gap:'var(--space-sm2)'}; opts {dark, tone:'tint'}
export function statusPill(status) // {background, color, borderRadius:999, padding:'4px 10px', fontSize:12, fontWeight:700} — AA-refined: done fg #15703c, failed fg #b03d12
export const GLYPHS             // guide '▤', briefing '✎', quiz '?', listen '♪', notebook '◍', arrow '→'
export function useViewport()    // 'phone' | 'tablet' | 'desktop' (SSR-safe; server snapshot = desktop)
```
CSS classes (globals.css from 02-01): `.btn .btn-primary .btn-secondary .btn-ghost .btn-link .btn-undone`, `.crumb-link`, `.skeleton`, `.only-phone .only-tablet .only-desktop`, `.skip-link`.
OWNERSHIP RULE (every element given a class): properties touched by :hover/:focus-visible/:active/[aria-current] rules (background, color, opacity, transform, outline) must NOT be set inline on that element — inline = layout (padding/grid/gap/width), class = state.
Responsive ladder (research Pattern 3): fluid styles → auto-fit/minmax grids → class helpers → useViewport() ONLY for structural DOM swaps (the sources table→cards swap qualifies; the 2-col tile grid does NOT — use `repeat(auto-fit, minmax(280px, 1fr))`).
Dev-server protocol: scratch port 3193 (never 3000/3100); verify with `node scripts/check-viewports.mjs --port 3193 --routes /subjects,/subjects/AI%20Agents,/subjects/DoesNotExist` (cookie fixture = onboarded) and system-Chrome headless captures for eyeballing.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Subjects index — responsive 2-col grid, h1, skeleton/empty/error states</name>
  <files>app/(dashboard)/subjects/page.js</files>
  <read_first>
  - .planning/phases/02-ui-design-responsive-overhaul/02-UI-SPEC.md — §12 row 4 (subjects index layouts), §14.3 (subjects index loading/empty/error copy — use VERBATIM), §9.6 (skeleton), §9.7 (empty state), §9.8 (error state), §6 (type roles: h1 = Display 32), §11.3 (one h1 per screen)
  - .planning/phases/02-ui-design-responsive-overhaul/02-CONTEXT.md — D-04 (360px acceptance), D-09 (microcopy), D-11 (regression fence: encoded hrefs stay)
  - app/(dashboard)/subjects/page.js (the file being rewritten — keep fetchSubjects with res.ok throw, the isError Retry branch shape, featured/others split, all Link hrefs)
  - node_modules/next/dist/docs/01-app/01-getting-started/05-server-and-client-components.md (this stays a 'use client' page fetching via useQuery — the established pattern)
  - .planning/phases/02-ui-design-responsive-overhaul/02-RESEARCH.md — Pattern 3 rungs 1–3 (grid auto-fit before JS), Pitfall 7 (do not drop Phase 1 wires while restyling)
  </read_first>
  <action>
Rewrite the page per UI-SPEC §12 row 4 and §14.3, keeping every Phase 1 navigation wire (D-11):

1. **Header:** the current `h2 "Subjects"` becomes the screen's single `<h1>` at Display 32 (`tokens.type.display` — fontSize 32, fontWeight 700, letterSpacing -1, lineHeight 1.15, color `var(--color-ink)`); the "{n} notebooks" meta stays Caption 12 `var(--color-text-secondary)`. Wrap the card content in the existing white card via `card()`.
2. **Responsive tiles:** keep the featured colored tile full-width; render the other subjects in `display:'grid', gridTemplateColumns:'repeat(auto-fit, minmax(280px, 1fr))', gap:'var(--space-sm2)'` (rung 2 — collapses 2-col tablet/desktop → 1-col phone with zero JS). Featured tile + list tiles tokenize per §9.10/§7.4: progress track `var(--color-on-color-chip)`-equivalent (keep current rgba white-on-color treatment — that IS the token value), amber fill `var(--color-amber)`, read-% pill via `statusPill`-style accent-tint pill (`tokens.color.accentTint` bg / `tokens.color.accentText` fg). Subject `tileColor` dots stay dynamic (D-06: subject accents are store-driven, not tokens).
3. **Loading (replaces bare `Loading…`):** 5 skeleton tiles — five `div className="skeleton"` blocks (~96px tall, r-lg) inside the card, container `aria-busy="true"`, plus an sr-only line (`position:absolute; left:-9999px` inline or a `<span className="sr-only">`-style inline) reading exactly "Sorting last night's haul…" (§14.3). Trigger: the existing `!subjects` branch.
4. **Empty:** when `subjects && subjects.length === 0` render the §9.7 idiom inside the card: 40px r-md icon tile (`var(--color-surface-tint)` bg, GLYPHS.notebook), Heading 20 "No subjects yet", Body secondary "They appear here as soon as your saved links get sorted. Nothing to do — keep saving.", Link `className="btn btn-link"` "Open your library →" → `/library` (§14.3 verbatim).
5. **Error (tokenize the existing branch):** `<div role="alert">` with Body danger-text "Couldn't load subjects — check the pipeline." (§14.3 — current copy already matches; keep it) + Retry as `<button className="btn btn-primary" style={pill('primary')} onClick={() => refetch()}>Retry</button>` (replaces the onClick span — D-11 hygiene).
6. Every remaining raw hex in the file becomes a token (`#fff` cards → `card()`, `#8b889f` → `var(--color-text-secondary)` per §7.6 ban on tertiary-for-small-text, `#f6f5fb` → `var(--color-surface-tint)`, borders → `var(--color-divider)`).
  </action>
  <verify>
    <automated>cd /Volumes/1TBFULLYPAID/Projects/yahshua/learnit && npm run build && grep -q "No subjects yet" app/\(dashboard\)/subjects/page.js && grep -q "Sorting last night's haul" app/\(dashboard\)/subjects/page.js && grep -q "className=\"skeleton\"" app/\(dashboard\)/subjects/page.js && grep -q "<h1" app/\(dashboard\)/subjects/page.js && grep -c "encodeURIComponent" app/\(dashboard\)/subjects/page.js && ! grep -q "Loading…" app/\(dashboard\)/subjects/page.js && echo SUBJECTS-INDEX-OK</automated>
  </verify>
  <done>Build passes; index shows h1 Subjects, 5-tile skeleton + sr-only copy while loading, verbatim empty/error copy, tokenized styling, auto-fit grid, and every subject Link still uses encodeURIComponent.</done>
  <acceptance_criteria>
  - `grep -c "encodeURIComponent" "app/(dashboard)/subjects/page.js"` ≥ 2 (featured + others hrefs preserved — D-11)
  - `grep -q "role=\"alert\"" "app/(dashboard)/subjects/page.js"` — error branch is announced
  - `! grep -q "Loading…" "app/(dashboard)/subjects/page.js"` — bare Loading… is gone (D-09)
  - Viewport sweep (dev server on port 3193): `node scripts/check-viewports.mjs --port 3193 --routes /subjects` reports NO overflow at 360/390/768/1024/1440; /tmp/shots/360-subjects.png shows the featured tile above 1-col list tiles
  - Fresh-profile capture at 360 (`"/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" --headless=new --disable-gpu --hide-scrollbars --window-size=360,800 --virtual-time-budget=8000 --screenshot=/tmp/02-03-subjects-360.png http://localhost:3193/subjects`) — with the gate retired (02-02), a fresh profile renders the SCREEN, not a redirect
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Subject detail — stacked phone layout, §9.5 sources cards, action-button idiom, tone states</name>
  <files>app/(dashboard)/subjects/[subjectId]/page.js</files>
  <read_first>
  - .planning/phases/02-ui-design-responsive-overhaul/02-UI-SPEC.md — §12 row 5 (subject detail layouts: hero flex-1 + 360px provenance desktop; all stacked phone; stats 2×2; materials 1-col; action pills wrap; sources table → §9.5 cards), §9.5 (table row → mobile card idiom), §9.9 (breadcrumb), §9.10 (hero), §14.3 (subject detail copy), §9.1 (button variants + not-yet-wired pill idiom), §7.4 (subject accent colors)
  - .planning/phases/02-ui-design-responsive-overhaul/02-CONTEXT.md — D-04, D-07 (one component vocabulary), D-11 (6 launcher Links + breadcrumb + 404 guard unchanged)
  - app/(dashboard)/subjects/[subjectId]/page.js (the file being rewritten — preserve fetchSubject null-on-404, `if (!name || subject === null) notFound()`, ALL six launcher hrefs, breadcrumb structure)
  - node_modules/next/dist/docs/01-app/03-api-reference/04-functions/not-found.md (notFound() render-path usage stays)
  - .planning/phases/02-ui-design-responsive-overhaul/02-RESEARCH.md — Pattern 3 rung 4 (useViewport for the table→cards DOM swap), Pitfall 7 (FLOW regression while restyling)
  </read_first>
  <action>
Rewrite per UI-SPEC §12 row 5, preserving every Phase 1 wire (D-11):

1. **Breadcrumb (§9.9):** "Subjects / {id}" — breadcrumb Links get `className="crumb-link"` (02-01 globals) and NO inline `color` (ownership rule); current page `{subject.id}` ink 700 Caption 12; separators `var(--color-text-faint)`; each crumb min-height 44 with 8px vertical padding.
2. **Action row:** "View pathway" stays a `Link className="btn btn-secondary" style={pill('secondary')}`; "Rename" and "Open in Gemini Notebook ↗" (currently dead spans) become `<button type="button" disabled className="btn btn-secondary btn-undone" style={pill('secondary')} title="Coming in a later update">` (§9.1 — full-opacity disabled, no faked function); "Rebuild material" becomes `<button type="button" disabled className="btn btn-primary btn-undone" style={pill('primary')} title="Coming in a later update">` (stays primary-weight per §12 row 5). On phone the row wraps (`flexWrap:'wrap'`), each pill keeps min-height 44.
3. **Hero (§9.10):** subject-colored (store `color` — dynamic accent, D-06/§7.4) r-2xl, padding xl desktop; eyebrow chip `NOTEBOOK · {n} SOURCES` on `var(--color-on-color-chip)`; h1 `{subject.id}` Display 32 (down from 44 — §6 absorption); blurb Body at `var(--color-on-color-soft)`; stat row becomes `display:'grid', gridTemplateColumns:'repeat(auto-fit, minmax(110px, 1fr))'` so it is 4-across desktop and 2×2 phone (rung 2); numerals Display 32 `lineHeight:1` (§6 exception). "Where it came from" card: 360px `flex:'none'` beside the hero ≥768; below it on phone (flexWrap or useViewport — the provenance card + hero sit in a flex row that wraps; give the card `flexBasis:360px, maxWidth:'100%'` so rung-1 wrapping stacks it).
4. **Material cards:** grid `repeat(auto-fit, minmax(240px, 1fr))` (3-across desktop → 1-col phone). Keep all six launcher Links with exact hrefs: Continue/Read again → `/pathway/${encodeURIComponent(subject.id)}`, ♪ Listen → `/listen?subject=…`, Retake/Review misses → `/quiz?subject=…`. Style launchers with `.btn .btn-primary`/`.btn .btn-ghost` + `pill()`.
5. **Sources table → §9.5:** import `useViewport` from `lib/tokens.js`. ≥tablet: current 4-col grid (`1fr 200px 120px 110px` → tighten gap to `var(--space-sm2)` on tablet) with Caption 12 uppercase headers. phone: render each SOURCE_ROWS row as a stacked card — `background:'var(--color-surface)'`… inside the parent white card use `var(--color-surface-tint)`, r-lg, padding 12, gap xs: line 1 = title (Body 14/700, `textOverflow:'ellipsis'`) + status pill right (`statusPill(row.status)`); line 2 = Caption meta `author · saved`; wrap in `role="list"`/`role="listitem"` (roles dropped consistently on phone per §9.5). Keep SOURCE_ROWS as-is (data realism is Phase 3 — DATA-01).
6. **States (§14.3):** loading → hero + 3 card skeletons ("Opening the notebook…" sr-only, `aria-busy`); error → tokenized existing branch with `role="alert"` and Retry as `.btn .btn-primary` button. Unknown-subject `notFound()` guard untouched.
7. Tokenize every remaining raw hex (borders `var(--color-border)`, `#e9e7f4` thumbs → `var(--color-surface-sunken)`, filter pills row header "All {n}" keeps accent-tint idiom).
  </action>
  <verify>
    <automated>cd /Volumes/1TBFULLYPAID/Projects/yahshua/learnit && npm run build && grep -q "useViewport" app/\(dashboard\)/subjects/\[subjectId\]/page.js && grep -q "crumb-link" app/\(dashboard\)/subjects/\[subjectId\]/page.js && grep -q "btn-undone" app/\(dashboard\)/subjects/\[subjectId\]/page.js && grep -q "Opening the notebook" app/\(dashboard\)/subjects/\[subjectId\]/page.js && grep -c "encodeURIComponent" app/\(dashboard\)/subjects/\[subjectId\]/page.js && grep -q "notFound()" app/\(dashboard\)/subjects/\[subjectId\]/page.js && echo SUBJECT-DETAIL-OK</automated>
  </verify>
  <done>Build passes; detail renders stacked phone layout with 2×2 hero stats and §9.5 source cards, actions follow the disabled-button idiom, all 6 launcher hrefs and the 404 guard are byte-identical in behavior.</done>
  <acceptance_criteria>
  - `grep -c "encodeURIComponent" "app/(dashboard)/subjects/[subjectId]/page.js"` ≥ 6 (all launchers preserved — D-11)
  - `grep -c "btn-undone" "app/(dashboard)/subjects/[subjectId]/page.js"` ≥ 3 (Rename, Gemini, Rebuild)
  - Viewport sweep: `node scripts/check-viewports.mjs --port 3193 --routes /subjects/AI%20Agents,/subjects/DoesNotExist` — no overflow at any width; the DoesNotExist capture shows the "Nothing here" 404 (guard intact)
  - /tmp/shots/360-subjects_AI_Agents.png shows: stacked hero with 2×2 stats, wrapped action pills, 1-col material cards, tinted source card rows (not a 4-col grid)
  - `grep -rn "SOURCE_ROWS" "app/(dashboard)/subjects/[subjectId]/page.js"` still present (no data rewiring — Phase 3 fence)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → GET /api/subjects, /api/subjects/[id] | read-only fetches of the in-memory store; subjectId is a URL-derived value passed to fetch with encodeURIComponent (existing) |
| client-rendered UI → no server mutation | this plan adds zero mutations; disabled action buttons cannot POST anything |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Tampering (XSS) | subject detail hero/blurb rendering | accept | All subject data (id, blurb) renders as JSX text — React auto-escapes; `grep -rn dangerouslySetInnerHTML app components` must stay empty; do not introduce it |
| T-02-03-02 | Tampering | subjectId URL segment → fetch path | accept | Existing encodeURIComponent on outbound fetch + decodeURIComponent in try/catch + null-on-404 → notFound() guard preserved verbatim (D-11); no new sink added |
| T-02-03-03 | Elevation | disabled Rename/Rebuild/Gemini buttons | accept | `disabled` + no onClick — they render but invoke nothing; title communicates "Coming in a later update"; Phase 4 wires them behind real handlers |
| T-02-03-04 | Repudiation | — | accept | No auth, no audit surface; single-user personal tool (unchanged) |
</threat_model>

<verification>
- `npm run build` passes.
- Viewport sweep (port 3193): `/subjects`, `/subjects/AI%20Agents`, `/subjects/DoesNotExist` clean at 360/390/768/1024/1440 (UI-03).
- D-11 spot re-checks for these two screens: all subject tile hrefs + 6 launchers still encodeURIComponent'd; unknown id renders 404 (client guard); the subjects index lists all 3 subjects from /api/subjects.
- Tone: loading shows skeletons + sr-only "Sorting last night's haul…"/"Opening the notebook…"; no bare "Loading…" remains (`grep -rn "Loading…" app` on these files is empty).
</verification>

<success_criteria>
- Both subjects screens are token-driven and pass the 360px no-overflow assertion.
- The §14.3 subjects copy table is implemented verbatim for loading/empty/error.
- Zero Phase 1 regressions: encoded hrefs, launcher Links, breadcrumb, 404 guard all intact.
- Actions that don't act yet are honest disabled buttons, not dead spans (D-11 hygiene).
</success_criteria>

<output>
After completion, create `.planning/phases/02-ui-design-responsive-overhaul/02-03-SUMMARY.md`
</output>
