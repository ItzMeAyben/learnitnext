---
phase: 02-ui-design-responsive-overhaul
plan: 05
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified:
  - app/(dashboard)/quiz/page.js
  - app/not-found.js
autonomous: true
requirements: [UI-03, UI-04, UI-05]
must_haves:
  truths:
    - "The quiz is fully usable at 360px: header wraps (context line, then progress bar + 'Question X of Y', 44px Exit), footer stacks (hint above full-width buttons), score cards stack — no horizontal scroll"
    - "Quiz options are real buttons ≥56px tall with visible focus rings; Skip and 'Next question' are .btn Ghost/Primary buttons, not onClick spans"
    - "The 404 page is token-styled and responsive: 420px card desktop, full-width with stacked buttons at 360px"
    - "Subject context still flows: ?subject= drives the breadcrumb, Exit returns to that subject, Play carries it to Listen (FLOW-04 unchanged)"
  artifacts:
    - path: "app/(dashboard)/quiz/page.js"
      provides: "Responsive quiz card with §14.3 skeleton/empty copy and button semantics"
      contains: "Question X of"
    - path: "app/not-found.js"
      provides: "System-joined responsive 404 (D-08)"
      contains: "Nothing here"
  key_links:
    - from: "app/(dashboard)/quiz/page.js"
      to: "app/(dashboard)/subjects/[subjectId]/page.js"
      via: "Exit Link backHref ternary from ?subject= (D-11 — unchanged logic)"
      pattern: "backHref"
    - from: "app/not-found.js"
      to: "/"
      via: "'Back to Today' Link (kept, tokenized)"
      pattern: "href=\"/\""
---

<objective>
Make the quiz screen responsive and semantic (§12 row 7: card full-bleed with md padding on phone, wrapped header, stacked footer, Heading-20 question per the §6 exception, options as buttons) and join the 404 page to the visual system (D-08): tokens, 420px → full-width phone card, stacked buttons.

Purpose: UI-03 (360px), UI-04 (quiz skeleton/empty copy from §14.3), UI-05 (404 joins the system — roadmap criterion 5). D-11 fence: ?subject= context, derived Exit/Play hrefs, and the Suspense boundary stay intact.

Output: rewritten `app/(dashboard)/quiz/page.js` and `app/not-found.js`. Quiz content stays the static QUIZ_ITEMS (DATA-02 is Phase 3).
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ui-design-responsive-overhaul/02-CONTEXT.md
@.planning/phases/02-ui-design-responsive-overhaul/02-RESEARCH.md
@app/(dashboard)/quiz/page.js
@app/not-found.js

<interfaces>
From lib/tokens.js (created in 02-01):
```js
export const tokens       // color/space/radius/type/status maps
export function pill(variant)   // layout-only; pair with className="btn btn-<variant>"
export function card(opts)
export const GLYPHS
```
CSS classes (globals.css from 02-01): `.btn .btn-primary .btn-secondary .btn-ghost .btn-undone`, `.crumb-link`, `.skeleton`, `.only-phone .only-tablet .only-desktop`.
OWNERSHIP RULE: no inline background/color/transform on elements carrying .btn-*/.crumb-link classes.
app/not-found.js is a SERVER component — it imports token values from lib/tokens.js directly (legal because tokens.js has no 'use client' directive).
Dev-server protocol: scratch port 3195; `node scripts/check-viewports.mjs --port 3195 --routes /quiz?subject=AI%20Agents,/nope` (note: pass the quiz route URL-encoded; the script handles query strings).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Quiz responsive + button semantics + §14.3 states</name>
  <files>app/(dashboard)/quiz/page.js</files>
  <read_first>
  - .planning/phases/02-ui-design-responsive-overhaul/02-UI-SPEC.md — §12 row 7 (quiz layouts at all three widths), §6 (typography EXCEPTION: quiz question uses Heading 20/700/lh 1.35, not Display), §9.1 (Skip=Ghost, Next question=Primary, Exit=Secondary), §10 (touch targets: quiz options already ~57px, keep ≥56), §11.3 (one h1), §14.3 (quiz copy)
  - .planning/phases/02-ui-design-responsive-overhaul/02-CONTEXT.md — D-04, D-07 (semantic buttons), D-09, D-11 (subject context + exits)
  - app/(dashboard)/quiz/page.js (the file being rewritten — keep Suspense wrapper, useSearchParams, backHref/playHref ternaries, picked/qIndex state, hint copy, graded option styling logic, QUIZ_ITEMS import)
  - node_modules/next/dist/docs/01-app/01-getting-started/04-linking-and-navigating.md (useSearchParams requires a Suspense boundary — the existing pattern stays)
  </read_first>
  <action>
Rewrite per §12 row 7, preserving the context machinery (D-11):

1. **Header:** wraps on phone — line 1: subject crumb (context `{subject ?? 'AI Agents'}` + "Quiz · 12 questions", `className="crumb-link"` where a Link exists, current page ink 700); line 2: flex row of progress bar (`flex:1, minWidth:0`, height 7→6px per §9.11, track `var(--color-surface-sunken)`, fill `var(--color-accent)`) + Caption "Question {qIndex} of {TOTAL_QUESTIONS}" + Exit as `Link className="btn btn-secondary" style={pill('secondary')}` (44px; href = existing backHref ternary — unchanged).
2. **Card:** `width:880, maxWidth:'100%'` stays; padding `var(--space-xl)` desktop → phone `var(--space-md)` via `clamp(16px, 5vw, 40px)` (rung 1). "MULTIPLE CHOICE" badge tokenizes to amber-tint/amber-text (`#8a5b00` fg per §7.2 AA refinement); source meta Caption `var(--color-text-secondary)`.
3. **Question:** h1 at Heading 20 / 700 / lineHeight 1.35 (§6 explicit exception — down from 30px; it remains the screen's single h1).
4. **Options:** convert each onClick div to `<button type="button" style={{...optionStyles, minHeight:56, textAlign:'left'}}>`. JS-driven graded states (correct green tint / picked-wrong danger tint, letter badge, ✓/✕ mark) stay inline — they are state-by-props, not pseudo-classes, so inline is legal. Buttons get the global :focus-visible ring for free. `cursor:'pointer'`, `font: inherit`.
5. **Footer:** stacks on phone — hint Caption above, then full-width button row (`flexDirection:'column'` phone / `'row'` desktop via flexWrap or `useViewport`; simplest: container `flexWrap:'wrap'` with buttons `flex:1 minWidth 200px`). "Skip" → `<button className="btn btn-ghost" style={pill('ghost')} onClick={() => setPicked(null)}>Skip</button>`; "Next question" → `<button className="btn btn-primary" style={pill('primary')} onClick={…existing advance logic…}>Next question</button>`.
6. **Score cards below:** grid `repeat(auto-fit, minmax(260px, 1fr))` — 2-across desktop, stacked phone. "Prefer to listen?" lime card keeps ink Primary "♪ Play" Link (`className="btn btn-primary" style={pill('primary')}`, href = existing playHref ternary — unchanged).
7. **States (§14.3, honest about no-fetch):** loading — replace the Suspense `fallback={null}` with a QuizSkeleton: card-shaped `.skeleton` block + 4 option-height `.skeleton` rows + sr-only "Dealing the questions…" (this fallback genuinely fires while useSearchParams suspends). Empty — add a guard branch `{QUIZ_ITEMS.length === 0 && <EmptyNoQuiz/>}` rendering "No quiz yet" + "Quizzes are built with your study guide — save a few more links first." (§9.7 idiom, Heading 20 + Body secondary; untriggerable with today's static data — it exists so Phase 3's real quiz data gets the right copy for free). Error — N/A this phase: the quiz has no fetch (a comment in the code notes the §14.3 error copy "Couldn't load the quiz — check the pipeline." + Retry lands with DATA-02's fetch in Phase 3); do NOT add a dead error branch.
8. Tokenize all remaining hex (borders `var(--color-border)`, graded greens/reds stay as literal state tints but sourced from tokens where a §7.2 name exists: correct tint `var(--color-success-tint)`, wrong tint `var(--color-danger-tint)`).
  </action>
  <verify>
    <automated>cd /Volumes/1TBFULLYPAID/Projects/yahshua/learnit && npm run build && grep -q "Dealing the questions" app/\(dashboard\)/quiz/page.js && grep -q "No quiz yet" app/\(dashboard\)/quiz/page.js && grep -q "btn-primary" app/\(dashboard\)/quiz/page.js && grep -q "btn-ghost" app/\(dashboard\)/quiz/page.js && grep -q "<button" app/\(dashboard\)/quiz/page.js && grep -q "Suspense" app/\(dashboard\)/quiz/page.js && grep -q "backHref" app/\(dashboard\)/quiz/page.js && echo QUIZ-OK</automated>
  </verify>
  <done>Build passes; quiz renders wrapped header, 56px button options, stacked footer and score cards at 360, Heading-20 question, verbatim §14.3 skeleton/empty copy, and unchanged context exits.</done>
  <acceptance_criteria>
  - `grep -c "onClick" "app/(dashboard)/quiz/page.js"` counts only button onClicks — `grep -c "<span[^>]*onClick\|<div[^>]*onClick" "app/(dashboard)/quiz/page.js"` returns 0 (no clickable divs/spans remain)
  - `grep -q "aria-pressed\|type=\"button\"" "app/(dashboard)/quiz/page.js"` (options are typed buttons)
  - Viewport sweep: `node scripts/check-viewports.mjs --port 3195 --routes "/quiz?subject=AI%20Agents"` — no overflow at 360–1440
  - Context check: `curl -s "http://localhost:3195/quiz?subject=Distribution" | grep -c "Distribution"` ≥ 1 and the Exit href renders `/subjects/Distribution` (FLOW-04 intact)
  - /tmp/shots/360-_quiz_subject=AI_Agents.png shows the stacked footer with full-width Skip/Next buttons
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: 404 joins the visual system (D-08) and goes responsive</name>
  <files>app/not-found.js</files>
  <read_first>
  - .planning/phases/02-ui-design-responsive-overhaul/02-UI-SPEC.md — §12 row 12 (404: centered 420px card desktop; card full-width margin md + stacked full-width buttons on phone), §9.1 (Back to Today = Primary, Browse subjects = Ghost), §9.2 (card), §6 (h1 Display 32), D-08 in 02-CONTEXT.md
  - app/not-found.js (the file being modified — keep the two Links and existing copy; it renders for both server 404s and client notFound() calls)
  - lib/tokens.js (token values importable by this server component)
  - node_modules/next/dist/docs/01-app/03-api-reference/04-functions/not-found.md (root not-found convention — stays a server component, no directive added)
  </read_first>
  <action>
Update `app/not-found.js` styling only (D-08):

1. Import `{ tokens, pill } from '../lib/tokens.js'` (server-safe). Keep it a server component.
2. Card: `card()` idiom — surface bg, `tokens.radius.xxl` (r-2xl reads friendlier than xl on a lone modal card and matches §12 row 12's intent; if judged off-system use radius.xl), `maxWidth:420, width:'100%'`, `margin:'var(--space-md)'` on phone via `margin:'clamp(16px, 4vw, 40px)'` auto-centering wrapper; gap `var(--space-sm2)`; text centered.
3. Copy stays: logo tile (ink, "L"), h1 "Nothing here" at Display 32, Body `var(--color-text-secondary)` line "This screen does not exist — the link may be old, or the subject was renamed."
4. Buttons: "Back to Today" → `Link href="/" className="btn btn-primary" style={pill('primary')}`; "Browse subjects" → `Link href="/subjects" className="btn btn-ghost" style={pill('ghost')}`. Phone: the pair stacks full-width — `display:'flex', flexDirection:{sm}…` cannot be media-queried inline, so use `flexWrap:'wrap'` with each Link `flex:1 1 160px` (stacks under ~360px content width, sits side-by-side desktop).
5. No other behavior changes — this file renders for unknown routes, unknown subjects (client notFound()), and unknown pathways.
  </action>
  <verify>
    <automated>cd /Volumes/1TBFULLYPAID/Projects/yahshua/learnit && npm run build && grep -q "tokens.js" app/not-found.js && grep -q "btn-primary" app/not-found.js && grep -q "btn-ghost" app/not-found.js && grep -q "Nothing here" app/not-found.js && ! grep -q "use client" app/not-found.js && echo X404-OK</automated>
  </verify>
  <done>Build passes; 404 card is token-styled with Primary/Ghost button pair that stacks on narrow widths; server-component status unchanged.</done>
  <acceptance_criteria>
  - `! grep -q "use client" app/not-found.js` — the 404 stays a server component (the verify asserts absence)
  - `curl -s http://localhost:3195/nope -o /dev/null -w "%{http_code}"` returns 404 and the body contains "Nothing here" + both Links
  - Viewport sweep includes `/nope` at 360 — no overflow; buttons stack
  - Cross-check: `/subjects/DoesNotExist` and `/pathway/DoesNotExist` still render this screen (client notFound() paths intact)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL query ?subject= → quiz render | client-controlled query string rendered as text and woven into hrefs (existing encodeURIComponent discipline) |
| browser → static 404 render | no data, no input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05-01 | Tampering (XSS) | ?subject= echoed into breadcrumb/hrefs | accept | Rendered as JSX text; every href composition keeps encodeURIComponent (backHref/playHref ternaries untouched — D-11); React escapes |
| T-02-05-02 | Tampering (open redirect) | Exit/Play hrefs derived from query | accept | hrefs are always prefix-anchored paths (`/subjects/…`, `/listen?subject=…`) — no absolute-URL passthrough exists or is added |
| T-02-05-03 | Elevation | quiz option/Skip/Next buttons | accept | Pure client state (picked/qIndex); no network calls added |
| T-02-05-04 | Information disclosure | 404 page | accept | Static copy only; never echoes the requested path |
</threat_model>

<verification>
- `npm run build` passes.
- Viewport sweep (port 3195): `/quiz?subject=AI%20Agents` and `/nope` clean at 360/390/768/1024/1440.
- D-11 spot re-checks: quiz Exit returns to `?subject=`'s subject; Play carries subject to /listen; 404 renders for unknown routes and unknown subject/pathway ids.
- Semantics: zero clickable div/span in quiz (options, Skip, Next are buttons); Exit/print-style links remain Links.
</verification>

<success_criteria>
- Quiz is thumb-usable at 360 with clear hierarchy and honest button semantics.
- 404 looks like the same product and works at every width (D-08 complete with 02-04's print half).
- No FLOW-04 regression: subject context in, subject context out.
</success_criteria>

<output>
After completion, create `.planning/phases/02-ui-design-responsive-overhaul/02-05-SUMMARY.md`
</output>
