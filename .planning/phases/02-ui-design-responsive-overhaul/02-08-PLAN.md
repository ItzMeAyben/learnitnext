---
phase: 02-ui-design-responsive-overhaul
plan: 08
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified:
  - components/TopNav.js
  - components/TabBar.js
  - components/PhoneHeader.js
  - components/AppShell.js
autonomous: true
requirements: [UI-03, UI-05]
must_haves:
  truths:
    - "On phones (<768px) every dashboard screen shows a fixed bottom tab bar with five working destinations (Today, Subjects, Listen, Library, Pipeline), each ≥44×48 with an inline-SVG icon and Caption label, the active tab ink-bold with a 20×3 accent bar — navigation is always thumb-reachable (D-05)"
    - "On phones the TopNav is display:none and a slim 56px inline header (logo mark + screen title) sits atop the content; on ≥768px the TopNav returns with its current proportions (D-05)"
    - "Tab order reads content → nav: the bottom bar is the LAST element in the AppShell DOM, after <main>; a 'Skip to content' link is the first tab stop (§11.2)"
    - "The active nav item is announced: aria-current=\"page\" on both the top tab and the bottom tab bar item"
    - "Every dashboard route's content clears the fixed bar — AppShell pads the content column with calc(56px + env(safe-area-inset-bottom)) on phone"
  artifacts:
    - path: "components/TabBar.js"
      provides: "Fixed phone bottom tab bar, 5 SVG-icon destinations, self-hiding ≥768 via .tabbar class"
      contains: "aria-current"
    - path: "components/PhoneHeader.js"
      provides: "56px phone inline header: logo mark + pathname-derived screen title"
      contains: "usePathname"
    - path: "components/TopNav.js"
      provides: "Tokenized desktop/tablet TopNav with .topnav class, .tab-item tabs, aria-current, 44px targets"
      contains: "tab-item"
    - path: "components/AppShell.js"
      provides: "Responsive shell: skip-link, TopNav/PhoneHeader swap, <main>, TabBar last, responsive padding + bottom inset"
      contains: "skip-link"
  key_links:
    - from: "components/AppShell.js"
      to: "components/TabBar.js"
      via: "rendered as the last child, after <main> (Pattern 4 DOM order)"
      pattern: "TabBar"
    - from: "components/TabBar.js"
      to: "/, /subjects, /listen, /library, /pipeline"
      via: "five Links mirroring TopNav's TABS (same match semantics, aria-current)"
      pattern: "href"
    - from: "app/(dashboard)/layout.js"
      to: "components/AppShell.js"
      via: "existing <AppShell>{children}</AppShell> from 02-02 (unchanged — this plan swaps AppShell's internals)"
      pattern: "AppShell"
---

<objective>
Deliver D-05's navigation adaptation and the shared responsive shell: extract the phone bottom tab bar (new `TabBar.js`), the phone inline header (new `PhoneHeader.js`), make `TopNav.js` tablet/desktop-only with token + class treatment and aria-current, and rework `AppShell.js` into the responsive chrome every dashboard screen lives in — skip-link first, TopNav/PhoneHeader swapped by breakpoint, `<main>` landmark, TabBar last in DOM, safe-area-aware bottom padding.

Purpose: UI-03 (app-like phone nav, no clipped chrome) and UI-05 (one nav vocabulary per §9.4). D-11 fence: the five destinations and their match semantics are identical to Phase 1's TopNav — this plan changes HOW nav renders per width, never WHERE it goes.

Output: rewritten `components/TopNav.js`, new `components/TabBar.js`, new `components/PhoneHeader.js`, reworked `components/AppShell.js`. Listen/Session render TabBar themselves in 02-11 (they live outside the (dashboard) group).
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ui-design-responsive-overhaul/02-CONTEXT.md
@.planning/phases/02-ui-design-responsive-overhaul/02-RESEARCH.md
@components/TopNav.js
@components/AppShell.js

<interfaces>
From lib/tokens.js (created in 02-01):
```js
export const tokens       // color/space/radius/type maps
export function useViewport()  // 'phone' | 'tablet' | 'desktop' — SSR snapshot 'desktop'
```
CSS classes (globals.css from 02-01 — ALL nav classes already exist there, do not edit globals.css):
- `.tabbar` — fixed bottom bar, height calc(56px + env(safe-area-inset-bottom)), safe-area padding, surface bg, shadow-bar, top hairline; `display:none` ≥768px
- `.tabbar-item` — ≥44×48 flex column center, Caption label; `[aria-current="page"]` → ink 700; `.tabbar-indicator` 20×3 accent bar (transparent at rest)
- `.topnav` — display:none <768px, restored ≥768px
- `.tab-item` — top nav tab: pill radius, padding 10px 20px, min-height 44; `[aria-current="page"]` → ink bg/white text/700; hover tint when not current
- `.skip-link`, `.btn .btn-secondary .btn-undone`, `.skeleton`
OWNERSHIP RULE: nav items take their state colors from the class layer — NO inline background/color on .tab-item/.tabbar-item carriers (layout-only inline: padding, gap, order).
Icons (UI-SPEC §1): hand-written inline SVGs, 24px, `currentColor`, stroke-width 1.8, no icon package: Today=house, Subjects=grid-of-4-squares, Listen=headphones (or note ♪-shaped path), Library=stacked-books/layers, Pipeline=arrow-flow (nodes + connecting line). Simple geometric paths are fine; they must read at 24px.
Dev-server protocol: scratch port 3198; `node scripts/check-viewports.mjs --port 3198 --routes /,/subjects,/library,/pipeline`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TabBar.js + PhoneHeader.js — the phone chrome</name>
  <files>components/TabBar.js, components/PhoneHeader.js</files>
  <read_first>
  - .planning/phases/02-ui-design-responsive-overhaul/02-UI-SPEC.md — §9.4 (bottom tab bar item spec: fixed bar 56px + inset, five items ≥44×48, SVG icon 24px + Caption 12 label, inactive text-secondary, active ink 700 with 20×3 accent bar, `<nav aria-label="Primary">`; phone inline header: logo mark + screen title, 56px), §12 "Common chrome" paragraph, §11.2/§11.3 (landmarks: second <nav aria-label>, hide the inactive bar with display:none per breakpoint — the .tabbar class does this)
  - .planning/phases/02-ui-design-responsive-overhaul/02-CONTEXT.md — D-05 (locked nav adaptation), discretion item "Bottom tab bar icon set"
  - components/TopNav.js (the TABS array + match functions to mirror EXACTLY — same five destinations, same match semantics)
  - .planning/phases/02-ui-design-responsive-overhaul/02-RESEARCH.md — Pattern 4 (TabBar placement: fixed positioning, DOM-last, aria-current via usePathname, safe-area padding) and its "Open Questions" resolution (TabBar is a self-contained client component, never in the root layout)
  - node_modules/next/dist/docs/01-app/01-getting-started/04-linking-and-navigating.md (Link + usePathname client pattern — established)
  </read_first>
  <action>
1. **components/TabBar.js** — `'use client'`. Import `Link` and `usePathname`. Define `TABS` mirroring TopNav's array verbatim (`{ label, to, match }` — Today `/` exact, others prefix). Each item renders `<Link href={tab.to} className="tabbar-item" aria-current={tab.match(pathname) ? 'page' : undefined} style={{ textDecoration:'none' }}>` containing: `<span className="tabbar-indicator" aria-hidden="true" />` (20×3, borderRadius 2, transparent → accent via the aria-current CSS rule), the 24px inline SVG (`viewBox="0 0 24 24"`, `fill="none"`, `stroke="currentColor"`, `strokeWidth="1.8"`, `strokeLinecap="round"`, `strokeLinejoin="round"`), and the Caption-12 label. Wrap all five in `<nav className="tabbar" aria-label="Primary">` with an inner flex row (`display:'flex', justifyContent:'space-around', alignItems:'stretch'`). NO inline background/color on the items (the class owns state); NO inline display/positioning of the bar (the .tabbar class owns fixed/hidden). The component ALWAYS renders — CSS hides it ≥768px (simpler than JS, and display:none removes it from a11y tree per §11.3).
2. **components/PhoneHeader.js** — `'use client'`. `usePathname()` → title via a small map: `/`→'Today', `/subjects*`→'Subjects', `/listen*`→'Listen', `/library`→'Library', `/pipeline`→'Pipeline', `/quiz*`→'Quiz', `/pathway*`→'Pathway', `/session`→'Session', fallback ''. Render a 56px row (`minHeight:56, display:'flex', alignItems:'center', gap:'var(--space-sm)', padding:'0 var(--space-md)', background:'var(--color-surface)', borderBottom:'1px solid var(--color-divider)'`): the 26px ink logo tile "L" (same idiom as TopNav) + `<span>` title at Heading-truncated-to-Body-700 ink (fontSize 16, fontWeight 700). Visible only on phone: wrap render in `useViewport() === 'phone' ? <header …/> : null` (JS is fine here — the header swap is structural).
3. Export both as default exports. No props.
  </action>
  <verify>
    <automated>cd /Volumes/1TBFULLYPAID/Projects/yahshua/learnit && npm run build && grep -q "aria-label=\"Primary\"" components/TabBar.js && grep -q "aria-current" components/TabBar.js && grep -q "tabbar-item" components/TabBar.js && grep -q "<svg" components/TabBar.js && grep -c "href" components/TabBar.js && grep -q "usePathname" components/PhoneHeader.js && echo PHONE-CHROME-OK</automated>
  </verify>
  <done>Build passes; TabBar renders five SVG-icon Links with aria-current and self-hides ≥768 via the class; PhoneHeader maps pathname to a title and renders only on phone.</done>
  <acceptance_criteria>
  - `grep -c "Link" components/TabBar.js` ≥ 5 (five destinations)
  - The five `to` values in TabBar.js are exactly `/`, `/subjects`, `/listen`, `/library`, `/pipeline` (D-11 — identical to TopNav's TABS)
  - All SVGs use stroke="currentColor" and strokeWidth="1.8" (UI-SPEC §1)
  - Placeholder render check: temporarily mounting is Task 2's job — here, `node -e "import('./components/TabBar.js')"`-style import is not meaningful for JSX; rely on build + Task 2 integration
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: TopNav tokenized + desktop-only; AppShell becomes the responsive shell</name>
  <files>components/TopNav.js, components/AppShell.js</files>
  <read_first>
  - .planning/phases/02-ui-design-responsive-overhaul/02-UI-SPEC.md — §9.4 (top tab spec: surface-sunken pill track, inactive text-secondary 500, active ink/white/700, padding 10px 20px, min-height 44, aria-current), §11.2 (skip-to-content link first tab stop, hidden until focused), §11.3 (landmarks: <header> for TopNav, second <nav aria-label="Primary">, <main> wrapping page content; hide inactive bar with display:none), §12 Common chrome (desktop/tablet TopNav; phone bottom bar + slim header)
  - .planning/phases/02-ui-design-responsive-overhaul/02-CONTEXT.md — D-05, D-07, D-11
  - components/TopNav.js (current implementation — keep TABS, logo idiom, right-side chrome)
  - components/AppShell.js (created in 02-02 as the plain server chrome — this task reworks it)
  - node_modules/next/dist/docs/01-app/01-getting-started/05-server-and-client-components.md (AppShell becomes a client component; server parents (dashboard)/layout.js and app/page.js may still render it — composition direction unchanged)
  - .planning/phases/02-ui-design-responsive-overhaul/02-RESEARCH.md — Pattern 4 (TabBar placement/DOM order), anti-pattern list (never root-layout TabBar), Pitfall 6 (safe-area inset — AppShell must add the padding because fixed elements don't push flow)
  </read_first>
  <action>
1. **components/TopNav.js** — add `'use client'` (already has it — keep). Wrap the outer div with `className="topnav"` and DROP any inline `display` that would fight the class's media display (the class hides <768 and restores ≥768; keep the existing inline flex styles — `display:flex` inline would override `display:none`! CRITICAL: the .topnav class sets `display:none` below 768 — an inline `display:'flex'` on the same element BEATS it per the ownership rule. Therefore the outer element takes the class for visibility, and the flex row moves to an INNER div that carries the layout styles). Tokenize: white → `var(--color-surface)`, radius 18 → nearest token `var(--radius-xl)` (20, per §5 absorption), `#f3f2f9` track → `var(--color-surface-sunken)`, `#5c5a70` → `var(--color-text-secondary)`, logo ink. Tabs become `className="tab-item"` Links with `aria-current={active ? 'page' : undefined}` and NO inline background/color/fontWeight (class owns active/hover state; keep only `textDecoration:'none'` inline). Pill-track padding 4 → keeps. Right-side chrome: the ⌕ search circle becomes `<button type="button" disabled className="btn btn-undone" title="Coming in a later update" aria-label="Search">` styled inline as the 36px circle (§9.1 — Search is a Phase 4 action); the ◔ notification dot and avatar chip stay decorative divs with `aria-hidden="true"`.
2. **components/AppShell.js** — becomes `'use client'` (it now calls useViewport). Structure:
   - `<a href="#main-content" className="skip-link">Skip to content</a>` — first element (the .skip-link class hides it until focused).
   - `<TopNav />` (class-hidden on phone — still in DOM, display:none per §11.3).
   - `<PhoneHeader />` (renders null on ≥phone... renders only when viewport==='phone').
   - `<main id="main-content" style={{ display:'flex', flexDirection:'column', gap:'var(--space-md2)', minWidth:0 }}>{children}</main>`.
   - `<TabBar />` — LAST child, after main (§11.2 tab order).
   - Column wrapper: `background:'var(--color-canvas)'` full-bleed, inner `maxWidth:1440, margin:'0 auto'`, padding via viewport: phone `'var(--space-md)'`, tablet `'var(--space-md2) var(--space-md)'`, desktop the Phase-1 `'26px 30px 40px'` → tokenized `'var(--space-md2) var(--space-lg2)'`… simpler and D-05-safe: desktop keeps `26px 30px` literal-equivalent via `padding:'26px 30px 40px'` unchanged (desktop proportions preserved verbatim). Add phone bottom clearance: `paddingBottom: 'calc(72px + env(safe-area-inset-bottom))'` ONLY on phone (content must clear the 56px fixed bar + inset).
   - `useViewport()` is called once at top; `minHeight:'100vh'` on the canvas wrapper stays.
3. Verify no inline style on TopNav's outer element or any .tab-item/.tabbar-item sets background/color/display (ownership).
4. Do NOT touch `app/(dashboard)/layout.js` or `app/page.js` — they already render `<AppShell>{children}</AppShell>` (02-02); the internals swap transparently. Listen/Session/landing/onboarding are NOT in AppShell (correct — 02-11 renders TabBar in Listen/Session directly; landing/onboarding show no bar per §12).
  </action>
  <verify>
    <automated>cd /Volumes/1TBFULLYPAID/Projects/yahshua/learnit && npm run build && grep -q "topnav" components/TopNav.js && grep -q "tab-item" components/TopNav.js && grep -q "aria-current" components/TopNav.js && grep -q "skip-link" components/AppShell.js && grep -q "main-content" components/AppShell.js && grep -q "TabBar" components/AppShell.js && grep -q "PhoneHeader" components/AppShell.js && grep -q "safe-area-inset-bottom" components/AppShell.js && echo SHELL-OK</automated>
  </verify>
  <done>Build passes; AppShell renders skip-link → TopNav/PhoneHeader → main → TabBar with responsive padding and bottom inset; TopNav is token/class-driven with aria-current tabs.</done>
  <acceptance_criteria>
  - Viewport sweep (port 3198, cookie fixture): `/subjects`, `/library`, `/pipeline` and `/` clean at 360–1440 — the shell adds no overflow at any width
  - /tmp/shots/360-subjects.png shows: PhoneHeader (logo + "Subjects"), content, bottom tab bar with Subjects active (accent bar); NO TopNav row
  - /tmp/shots/1024-subjects.png shows the TopNav pill row and NO bottom bar (D-05 both directions)
  - Keyboard order check (playwright-core `page.keyboard` Tab walk at 360 on /subjects): first focus = skip-link visible on focus, content links reachable, the five tab-bar Links come AFTER the content links (DOM order)
  - `grep -q "aria-label=\"Primary\"" components/TabBar.js` — second landmark nav exists alongside TopNav's header landmark
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client pathname → active-tab computation | usePathname is framework-provided; used only to set aria-current/match booleans |
| nav Links → internal routes | five hardcoded internal hrefs, identical to Phase 1's TopNav (D-11) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08-01 | Tampering (XSS) | pathname-derived PhoneHeader title | accept | Title comes from a hardcoded pathname→string map, never from the URL value itself; unknown paths render '' |
| T-02-08-02 | Elevation | disabled search button in TopNav | accept | Disabled, no handler, title tooltip; Phase 4 wires it |
| T-02-08-03 | Spoofing | — | accept | No user input, no data rendering in this plan; purely navigational chrome |
| T-02-08-04 | Denial of Service | fixed TabBar overlapping content | mitigate | AppShell's phone-only bottom padding `calc(72px + env(safe-area-inset-bottom))` guarantees clearance; verified by the 360px screenshots (last content row visible above the bar) |
</threat_model>

<verification>
- `npm run build` passes.
- Viewport sweep (port 3198): `/`, `/subjects`, `/library`, `/pipeline` clean at 360/390/768/1024/1440 — shell change is global, so sweep multiple routes.
- D-11: all five destinations reachable at BOTH widths (top tabs ≥768, bottom bar <768); Today match is exact-`/` only (no false-active on /library).
- A11y: skip-link first tab stop; both navs landmarked; inactive bar display:none (not just visually hidden).
</verification>

<success_criteria>
- Phone feels app-like: thumb bar, slim header, content clears the bar, safe-area respected (notch flagged for human UAT — headless has no notch).
- Desktop is pixel-faithful to Phase 1 proportions (D-05).
- One nav vocabulary: same five labels, same icons semantics, same active idiom top and bottom.
</success_criteria>

<output>
After completion, create `.planning/phases/02-ui-design-responsive-overhaul/02-08-SUMMARY.md`
</output>
