---
phase: 01-connect-screens
plan: 3
type: execute
wave: 1
depends_on: []
files_modified:
  - components/TopNav.js
  - app/(dashboard)/subjects/page.js
  - app/(dashboard)/page.js
autonomous: true
requirements:
  - FLOW-03
  - FLOW-05
must_haves:
  truths:
    - "All five TopNav tabs navigate: Today /, Subjects /subjects, Listen /listen, Library /library, Pipeline /pipeline"
    - "The Subjects tab opens a subjects index screen that lists every subject from /api/subjects"
    - "Every subject on the index links to its /subjects/[id] detail page with an encoded href"
    - "The Home Finance tile navigates to /subjects instead of being a dead span"
    - "The /session screen is reachable from the Today screen"
  artifacts:
    - path: "app/(dashboard)/subjects/page.js"
      provides: "New subjects index screen at /subjects"
      contains: "/api/subjects"
      min_lines: 40
    - path: "components/TopNav.js"
      provides: "Global nav with working Subjects tab"
      contains: "to: '/subjects'"
    - path: "app/(dashboard)/page.js"
      provides: "Today screen with Finance tile link, fresh-material links, session entry"
      contains: 'href="/session"'
  key_links:
    - from: "components/TopNav.js"
      to: "app/(dashboard)/subjects/page.js"
      via: "Subjects tab href /subjects"
      pattern: "to: '/subjects'"
    - from: "app/(dashboard)/subjects/page.js"
      to: "/api/subjects"
      via: "useQuery fetchSubjects"
      pattern: "useQuery"
    - from: "app/(dashboard)/subjects/page.js"
      to: "app/(dashboard)/subjects/[subjectId]/page.js"
      via: "per-subject Link with encodeURIComponent"
      pattern: "encodeURIComponent"
---

<objective>
Complete the app's primary navigation skeleton (D-03, D-04): TopNav reaches all five
destinations, a NEW `/subjects` index screen lists every subject, every hardcoded
`/subjects/AI Agents` nav target points at the index, the Home screen's dead
navigation spans become working links (D-05 home portion), and `/session` gains an
entry point from the primary interface (D-06).

Purpose: FLOW-03 (destinations + index) and the Home part of FLOW-05. Today the
Subjects tab points at a hardcoded unencoded `/subjects/AI Agents`, no index screen
exists, and the Finance tile is a dead span.
Output: Fixed `components/TopNav.js`, new `app/(dashboard)/subjects/page.js`, wired
`app/(dashboard)/page.js`.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-connect-screens/01-CONTEXT.md

<interfaces>
<!-- Existing contracts the executor replicates. Extracted from codebase. -->

components/TopNav.js TABS (lines 6-12) — only `to` changes, `match` predicates already
handle both `/subjects` and `/subjects/X`:
```js
const TABS = [
  { label: 'Today', to: '/', match: (p) => p === '/' },
  { label: 'Subjects', to: '/subjects/AI Agents', match: (p) => p.startsWith('/subjects') },  // ← fix to: '/subjects'
  { label: 'Listen', to: '/listen', match: (p) => p.startsWith('/listen') },
  { label: 'Library', to: '/library', match: (p) => p.startsWith('/library') },
  { label: 'Pipeline', to: '/pipeline', match: (p) => p.startsWith('/pipeline') },
]
```

app/(dashboard)/page.js — data-fetch + card idioms to replicate on the index:
```js
async function fetchSubjects() {
  const res = await fetch('/api/subjects')
  return res.json()
}
const { data: subjects } = useQuery({ queryKey: ['subjects'], queryFn: fetchSubjects })
```
Featured card idiom (lines 80-100): full-card Link, `background: subject.color,
borderRadius: 18, padding: 18, color: '#fff', textDecoration: 'none'` with a
readPct progress bar row. Row card idiom (lines 103-117): full-row Link
`background: '#f6f5fb', borderRadius: 16, padding: '16px 18px'` with a 10px
tileColor dot, name, `{sourceCount} sources · {readPct}% read`, right readPct badge.

app/(dashboard)/layout.js: renders `<TopNav />` above `{children}` — the new index page
automatically inherits the TopNav chrome by living in the `(dashboard)` route group
(no URL prefix).

Home dead-span targets verified in 01-RESEARCH.md inventory:
- Finance tile `app/(dashboard)/page.js` lines 120-126 (plain div + dead → arrow).
- FRESH_MATERIAL cards lines 30-35 + 137-148 (plain divs naming subjects/docs).
- "Save a link" / "Run now" spans lines 66-67: ACTION controls — DO NOT TOUCH (D-05).

lib/store.js SUBJECTS ids: 'AI Agents', 'Distribution', 'Sales' (subject ids contain
spaces — every dynamic href must use encodeURIComponent).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Point the TopNav Subjects tab at /subjects and build the subjects index screen</name>
  <read_first>
  - components/TopNav.js (file being modified — TABS array lines 6-12)
  - app/(dashboard)/page.js (card idioms + fetchSubjects/useQuery pattern to replicate)
  - app/(dashboard)/layout.js (confirms the (dashboard) route group renders TopNav for the new page)
  - app/(dashboard)/subjects/[subjectId]/page.js (the screen each index card links to; its Loading… idiom)
  - .planning/phases/01-connect-screens/01-RESEARCH.md (Pitfall 4 space-containing ids; Pattern 1 dead span → Link)
  - node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/route-groups.md (D-12: group semantics, no URL impact)
  </read_first>
  <files>components/TopNav.js, app/(dashboard)/subjects/page.js</files>
  <action>
1. `components/TopNav.js` (D-03, D-04): change the Subjects tab entry to
   `{ label: 'Subjects', to: '/subjects', match: (p) => p.startsWith('/subjects') }`.
   Nothing else in the file changes. This also fixes one of the two hardcoded
   `/subjects/AI Agents` hrefs in the codebase (the other is Plan 04's quiz Exit) and
   the unencoded-space href bug (FLOW-06 encoding half).

2. Create `app/(dashboard)/subjects/page.js` (D-04) — a NEW client index screen in the
   existing visual language (D-11), same card/list idioms as Today's subject cards:
   - `'use client'`; `import Link from 'next/link'`; `import { useQuery } from '@tanstack/react-query'`.
   - Copy the `fetchSubjects` helper verbatim from `app/(dashboard)/page.js`.
   - Page structure inside a single white card (`background: '#fff', borderRadius: 22, padding: 24`):
     - Header row: `<h2 style={{ margin: 0, fontSize: 20, fontWeight: 700, letterSpacing: '-.4px', color: '#12121a' }}>Subjects</h2>`
       and `<span style={{ fontSize: 12, color: '#8b889f' }}>{subjects?.length ?? 0} notebooks</span>`.
     - Loading state: `if (!subjects) return <div style={{ padding: 40, color: '#8b889f' }}>Loading…</div>`
       (same idiom as the subject detail page).
   - First subject = featured hero card, replicating Home's featured card markup
     (lines 80-100 of `app/(dashboard)/page.js`) WITHOUT the "New today" badge:
     full-card `<Link href={`/subjects/${encodeURIComponent(subject.id)}`}
     style={{ background: subject.color, borderRadius: 18, padding: 18, color: '#fff',
     display: 'flex', flexDirection: 'column', gap: 14, textDecoration: 'none' }}>`
     containing the subject name at `fontSize: 18, fontWeight: 700` plus the same
     readPct progress bar + `{readPct}% · {sourceCount} sources` footer row.
   - Remaining subjects = row cards, replicating Home's "others" row markup
     (lines 103-117) exactly: full-row Link, `background: '#f6f5fb', borderRadius: 16,
     padding: '16px 18px', textDecoration: 'none'`, 10px `tileColor` dot, name
     (`fontSize: 15, fontWeight: 600, color: '#12121a'`),
     `{sourceCount} sources · {readPct}% read` (`fontSize: 12, color: '#8b889f'`), and
     the right-side readPct badge (`background: '#efe9ff', color: '#5b2fd0', borderRadius: 999,
     padding: '4px 10px', fontSize: 11, fontWeight: 700`).
   - Every href is built with `encodeURIComponent(subject.id)` — subject ids contain
     spaces ("AI Agents") and a raw space in an href is the exact bug this phase
     eradicates (research Pitfall 4).
  </action>
  <verify>
  <automated>npm run build && grep -q "to: '/subjects'" components/TopNav.js && ! grep -q "AI Agents" components/TopNav.js && grep -q "useQuery" "app/(dashboard)/subjects/page.js" && grep -q "encodeURIComponent" "app/(dashboard)/subjects/page.js" && grep -q "Loading…" "app/(dashboard)/subjects/page.js"</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c "'/listen'" components/TopNav.js` >= 1, `grep -c "'/library'" components/TopNav.js` >= 1, `grep -c "'/pipeline'" components/TopNav.js` >= 1 (all five destinations present, D-03)
  - `! grep -q "AI Agents" components/TopNav.js` (hardcoded unencoded target gone)
  - `app/(dashboard)/subjects/page.js` exists; `grep -c "encodeURIComponent" "app/(dashboard)/subjects/page.js"` >= 2 (featured + row hrefs)
  - `grep "<Link" "app/(dashboard)/subjects/page.js"` matches (cards are Links, not divs)
  - With `npm run dev` running: `curl -s -o /dev/null -w '%{http_code}' http://localhost:3000/subjects` outputs `200` AND `curl -s http://localhost:3000/subjects` output contains `Loading…` (client page — subject names arrive via TanStack Query AFTER hydration, so SSR HTML shows the loading state, never the names; same curl caveat as Plan 05's client pages). Names verified in-browser: after hydration the list renders `AI Agents`, `Distribution`, and `Sales`.
  - `npm run build` exits 0
  </acceptance_criteria>
  <done>TopNav Subjects tab opens /subjects; the index lists all three subjects from /api/subjects, each card navigating to its encoded detail URL; loading state present.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Home's dead navigation spans (Finance tile, fresh-material cards) and add the Session entry</name>
  <read_first>
  - app/(dashboard)/page.js (file being modified — Finance tile lines 120-126, FRESH_MATERIAL lines 30-35 and 137-148, header actions row lines 65-68)
  - app/session/page.js (what the Session screen is — the destination of the new entry affordance)
  - app/(dashboard)/subjects/page.js (created in Task 1 — where /subjects now leads)
  - .planning/phases/01-connect-screens/01-CONTEXT.md (D-05 offender list; D-06 session integration discretion; action buttons explicitly OUT)
  - .planning/phases/01-connect-screens/01-RESEARCH.md (Pattern 1 "Dead span → styled Link"; anti-pattern "nesting a Link inside a Link")
  </read_first>
  <files>app/(dashboard)/page.js</files>
  <action>
Convert Home's navigation-labeled dead spans to styled Links (D-05, FLOW-05),
preserving every inline style byte-for-byte and adding `textDecoration: 'none'`
(D-11). ACTION controls ("Save a link" line 66, "Run now" line 67) stay untouched
spans — they are Phase 3 (CAPT-01/02) work.

1. Finance tile (lines 120-126, D-05): the whole row div becomes
   `<Link href="/subjects" style={{ background: '#f6f5fb', borderRadius: 16, padding: '16px 18px', display: 'flex', alignItems: 'center', justifyContent: 'space-between', textDecoration: 'none' }}>`
   — same children (title, "3 sources · needs 2 more", → arrow span). Finance is not
   in SUBJECTS, so it points at the subjects index (research inventory guidance),
   which after Plan 02 makes `/subjects/Finance` a 404 — the index is the correct
   non-dead target.

2. FRESH_MATERIAL cards (research assumption A3, planner discretion — satisfies
   FLOW-05 "no dead spans on Home"): add a `href` to each entry in the FRESH_MATERIAL
   array (lines 30-35) with these concrete values:
   - `Study guide` → `href: '/subjects/AI%20Agents'`
   - `Briefing doc` → `href: '/subjects/Distribution'`
   - `Quiz · 12 questions` → `href: '/quiz?subject=AI%20Agents'`
   - `Audio episode` → `href: '/listen?subject=Sales'`
   Then convert the card `<div key={item.title} ...>` (lines 137-148) to
   `<Link key={item.title} href={item.href} style={{ ...sameStyles, textDecoration: 'none' }}>`.
   (These are literal encoded hrefs, not interpolations — no encodeURIComponent needed.
   `?subject=AI%20Agents` decodes to `AI Agents` via URLSearchParams in Plan 04's readers.)
   The quiz/listen pages read `?subject=` in Plan 04 (same wave) — links landing
   before that lands simply keep current defaults, never break.

3. Session entry (D-06, discretion = Today link): add ONE pill-style Link as the first
   item inside the header actions flex row (line 65's `<div style={{ display: 'flex', gap: 10 }}>`):
   `<Link href="/session" style={{ background: '#fff', borderRadius: 999, padding: '11px 18px', fontSize: 13.5, fontWeight: 600, color: '#12121a', textDecoration: 'none' }}>Session view</Link>`
   (matches the existing white-pill idiom of "Save a link" exactly). Session's own
   internal links are wired by Plan 04.

4. Anti-pattern guard (research): the featured and "others" subject cards (lines 80-118)
   are already full-card Links — do NOT add nested Links inside them while editing
   nearby markup.
  </action>
  <verify>
  <automated>npm run build && grep -q 'href="/session"' "app/(dashboard)/page.js" && grep -q "/subjects/AI%20Agents" "app/(dashboard)/page.js" && grep -q "/quiz?subject=AI%20Agents" "app/(dashboard)/page.js" && grep -q "/listen?subject=Sales" "app/(dashboard)/page.js" && grep -q 'href="/subjects"' "app/(dashboard)/page.js"</automated>
  </verify>
  <acceptance_criteria>
  - The Finance block (grep -A3 ">Finance<" on the file) is inside a `<Link href="/subjects"` element, not a plain div
  - `grep -c "href:" "app/(dashboard)/page.js"` reflects 4 FRESH_MATERIAL hrefs; the FRESH_MATERIAL card renderer uses `<Link` (grep the map body)
  - `grep "Session view" "app/(dashboard)/page.js"` matches inside a Link with href="/session"
  - `grep -n "Save a link" "app/(dashboard)/page.js"` shows it STILL a `<span` (action controls untouched), same for "Run now"
  - `npm run build` exits 0
  </acceptance_criteria>
  <done>Home has zero navigation-labeled dead spans: Finance tile → /subjects, all four fresh-material cards navigate, Session is reachable via a "Session view" pill, and action buttons remain visually unchanged spans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Store data (subject names) → href construction | Subject ids with spaces flow into Link hrefs |
| No new network trust boundary | Index screen reads the existing read-only /api/subjects |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | subject ids interpolated into hrefs | accept | All dynamic hrefs built with `encodeURIComponent` (specified in-task); subject ids come from the app's own in-memory store, not user input; React escapes all rendered text |
| T-03-02 | Spoofing | /api/subjects consumer | accept | Read-only public mock endpoint; no credentials or PII involved |
| T-03-03 | Repudiation | nav wiring | accept | Pure navigation; no state-changing action to repudiate |
| T-03-04 | Information disclosure | subjects index | accept | Lists non-sensitive mock data (subject names, counts) already public on Today |
| T-03-05 | DoS | subjects index | accept | Single useQuery against an in-memory array of 3; no unbounded rendering |
| T-03-06 | Elevation of privilege | TopNav/index | accept | Adds only navigation; no privileged destination exists |
</threat_model>

<verification>
- `npm run build` exits 0.
- `grep -rn "AI Agents" components/TopNav.js` returns nothing (unencoded href eradicated from the nav).
- Dev-server walkthrough: TopNav Subjects tab → index lists AI Agents / Distribution /
  Sales; Finance tile → /subjects; a fresh-material Quiz card → /quiz?subject=AI%20Agents;
  Session view pill → /session (dark session chrome renders).
- FLOW-05 / roadmap criterion 6 record (Library + Pipeline): both screens were
  inventoried and contain NO navigation-labeled dead spans — their only dead spans are
  D-05-excluded action controls (`grep -n "<span" "app/(dashboard)/library/page.js"`
  → Search transcripts / Export CSV / Save a link, lines 196-204;
  `grep -n "<span" "app/(dashboard)/pipeline/page.js"` → Schedule ▾ / Rename / Copy log,
  plus the working onClick run control). Criterion 6 is satisfied vacuously; no plan in
  this phase modifies these two screens' spans.
</verification>

<success_criteria>
- FLOW-03: all five destinations reachable from TopNav; Subjects opens a real index
  listing all subjects (not a hardcoded subject page).
- FLOW-05 (Home portion): Finance tile and fresh-material cards are working links;
  no navigation-labeled dead span remains on Home.
- D-06 entry half: /session reachable from Today.
- D-11: every converted control renders identically (same inline styles + `textDecoration: 'none'`).
</success_criteria>

<output>
After completion, create `.planning/phases/01-connect-screens/01-03-SUMMARY.md`
</output>
